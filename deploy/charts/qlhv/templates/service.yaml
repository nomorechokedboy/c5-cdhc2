apiVersion: v1
kind: Service
metadata:
  name: {{ include "qlhv.fullname" . }}
  labels:
    {{- include "qlhv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qlhv.selectorLabels" . | nindent 4 }}
