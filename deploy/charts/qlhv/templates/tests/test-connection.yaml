apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qlhv.fullname" . }}-test-connection"
  labels:
    {{- include "qlhv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qlhv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
