apiVersion: v1
kind: Service
metadata:
  name: {{ include "sms.fullname" . }}-api
  labels:
    {{- include "sms.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sms.api.selectorLabels" . | nindent 4 }}
