apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sms.fullname" . }}-test-web-connection"
  labels:
    {{- include "sms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sms.fullname" . }}-web:{{ .Values.web.service.port }}']
  restartPolicy: Never

