// Code generated by the Encore v1.57.9 client generator. DO NOT EDIT.

// Disable eslint, jshint, and jslint for this file.
/* eslint-disable */
/* jshint ignore:start */
/*jslint-disable*/

/**
 * BaseURL is the base URL for calling the Encore application's API.
 */
export type BaseURL = string

export const Local: BaseURL = 'http://localhost:4000'

/**
 * Environment returns a BaseURL for calling the cloud environment with the given name.
 */
export function Environment(name: string): BaseURL {
	return `https://${name}-xyzwe.encr.app`
}

/**
 * PreviewEnv returns a BaseURL for calling the preview environment with the given PR number.
 */
export function PreviewEnv(pr: number | string): BaseURL {
	return Environment(`pr${pr}`)
}

const BROWSER = typeof globalThis === 'object' && 'window' in globalThis

/**
 * Client is an API client for the xyzwe Encore application.
 */
export default class Client {
	public readonly actions: actions.ServiceClient
	public readonly asset_management: asset_management.ServiceClient
	public readonly auth: auth.ServiceClient
	public readonly classes: classes.ServiceClient
	public readonly exam_management: exam_management.ServiceClient
	public readonly healthcheck: healthcheck.ServiceClient
	public readonly leave_management: leave_management.ServiceClient
	public readonly media: media.ServiceClient
	public readonly notifications: notifications.ServiceClient
	public readonly permissions: permissions.ServiceClient
	public readonly resources: resources.ServiceClient
	public readonly roles: roles.ServiceClient
	public readonly students: students.ServiceClient
	public readonly units: units.ServiceClient
	public readonly user_roles: user_roles.ServiceClient
	public readonly users: users.ServiceClient
	private readonly options: ClientOptions
	private readonly target: string

	/**
	 * Creates a Client for calling the public and authenticated APIs of your Encore application.
	 *
	 * @param target  The target which the client should be configured to use. See Local and Environment for options.
	 * @param options Options for the client
	 */
	constructor(target: BaseURL, options?: ClientOptions) {
		this.target = target
		this.options = options ?? {}
		const base = new BaseClient(this.target, this.options)
		this.actions = new actions.ServiceClient(base)
		this.asset_management = new asset_management.ServiceClient(base)
		this.auth = new auth.ServiceClient(base)
		this.classes = new classes.ServiceClient(base)
		this.exam_management = new exam_management.ServiceClient(base)
		this.healthcheck = new healthcheck.ServiceClient(base)
		this.leave_management = new leave_management.ServiceClient(base)
		this.media = new media.ServiceClient(base)
		this.notifications = new notifications.ServiceClient(base)
		this.permissions = new permissions.ServiceClient(base)
		this.resources = new resources.ServiceClient(base)
		this.roles = new roles.ServiceClient(base)
		this.students = new students.ServiceClient(base)
		this.units = new units.ServiceClient(base)
		this.user_roles = new user_roles.ServiceClient(base)
		this.users = new users.ServiceClient(base)
	}

	/**
	 * Creates a new Encore client with the given client options set.
	 *
	 * @param options Client options to set. They are merged with existing options.
	 **/
	public with(options: ClientOptions): Client {
		return new Client(this.target, {
			...this.options,
			...options
		})
	}
}

/**
 * ClientOptions allows you to override any default behaviour within the generated Encore client.
 */
export interface ClientOptions {
	/**
	 * By default the client will use the inbuilt fetch function for making the API requests.
	 * however you can override it with your own implementation here if you want to run custom
	 * code on each API request made or response received.
	 */
	fetcher?: Fetcher

	/** Default RequestInit to be used for the client */
	requestInit?: Omit<RequestInit, 'headers'> & {
		headers?: Record<string, string>
	}

	/**
	 * Allows you to set the authentication data to be used for each
	 * request either by passing in a static object or by passing in
	 * a function which returns a new object for each request.
	 */
	auth?: auth.AuthParams | AuthDataGenerator
}

export namespace actions {
	export interface GetActionsResponse {
		data: schema.Action[]
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.GetActions = this.GetActions.bind(this)
		}

		public async GetActions(): Promise<GetActionsResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI('GET', `/actions`)
			return (await resp.json()) as GetActionsResponse
		}
	}
}

export namespace asset_management {
	export interface AccountAuditLogResponse {
		id: number
		createdAt: string
		action: string
		actorUserId: number | null
		actorUsername: string | null
		actorDisplayName: string | null
		actorIsAdmin: boolean
		roomId: number | null
		roomCode: string | null
		roomName: string | null
		address: string | null
		floorName: string | null
		buildingCode: string | null
		buildingName: string | null
		accountLabel: string | null
		summary: string
		details: string | null
	}

	export interface AssetBrokenLogResponse {
		id: number
		createdAt: string
		updatedAt: string
		eventType: string
		sourceType: string
		sourceId: number | null
		proposalId: number | null
		repairRequestId: number | null
		roomAssetId: number | null
		sourceAssetId: number | null
		assetCode: string | null
		originalCode: string | null
		assetName: string
		category: string | null
		quantity: number
		originalGrade: number | null
		gradeAfter: number | null
		statusAfter: string | null
		roomId: number | null
		roomCode: string | null
		roomName: string | null
		floorName: string | null
		buildingCode: string | null
		buildingName: string | null
		unitName: string | null
		nganhCode: string | null
		reason: string | null
		resultNote: string | null
		performer: string | null
		eventAt: string
		actorUserId: number | null
		actorUsername: string | null
		actorDisplayName: string | null
	}

	export interface AssetCatalogResponse {
		/**
		 * Ngành quản lý (HC2A, HC2B…)
		 */
		nganh: CatalogCategory[]

		/**
		 * Chuyên ngành (HC2A01…)
		 */
		chuyenNganh: CatalogCategory[]

		materials: CatalogMaterial[]
	}

	/**
	 * Asset row with location + repair lifecycle (used in stats drill-down & broken report)
	 */
	export interface AssetDetailRow {
		id: number
		code: string | null
		name: string
		category: string
		quantity: number
		unit: string | null
		grade: number
		status: string
		purchaseDate: string | null
		expiryDate: string | null
		description: string | null
		brokenAt: string | null
		repairStartedAt: string | null
		repairCompletedAt: string | null
		repairPerformer: string | null
		/**
		 * true if repairCompletedAt is set
		 */
		repairCompleted: boolean

		roomId: number
		roomCode: string
		roomName: string
		floorId: number
		floorName: string
		buildingId: number
		buildingCode: string
		buildingName: string
	}

	export interface AssetMovementReportResponse {
		roomId: number
		roomCode: string
		roomName: string
		floorId: number
		floorName: string
		buildingId: number
		buildingCode: string
		buildingName: string
		/**
		 * Đơn vị sử dụng (holding) của VT lúc ghi log / hiện tại
		 */
		holdingUnitId: number | null

		id: number
		createdAt: string
		updatedAt: string
		roomAssetId: number
		movementType: string
		executedAt: string
		executingUnit: string | null
		installAddress: string | null
		assetCode: string | null
		assetName: string
		quantity: number
		quantityBefore: number
		quantityAfter: number
		grade: number
		manufactureYear: number | null
		usageYear: number | null
		reasonCode: string | null
		reasonOther: string | null
		decisionDate: string | null
		decisionNumber: string | null
		signer: string | null
		performer: string | null
		explanation: string | null
		note: string | null
	}

	export interface AssetMovementResponse {
		id: number
		createdAt: string
		updatedAt: string
		roomAssetId: number
		movementType: string
		executedAt: string
		executingUnit: string | null
		installAddress: string | null
		assetCode: string | null
		assetName: string
		quantity: number
		quantityBefore: number
		quantityAfter: number
		grade: number
		manufactureYear: number | null
		usageYear: number | null
		reasonCode: string | null
		reasonOther: string | null
		decisionDate: string | null
		decisionNumber: string | null
		signer: string | null
		performer: string | null
		explanation: string | null
		note: string | null
	}

	export interface AssetStatsReport {
		totalAssets: number
		totalQuantity: number
		byStatus: {
			status: string
			count: number
			quantity: number
			/**
			 * Thiết bị thuộc trạng thái này
			 */
			items: AssetDetailRow[]
		}[]
		byCategory: {
			category: string
			count: number
			quantity: number
			/**
			 * Thiết bị thuộc loại này
			 */
			items: AssetDetailRow[]
		}[]
		byBuilding: {
			buildingId: number
			buildingCode: string
			buildingName: string
			count: number
			quantity: number
		}[]
	}

	export type BrokenAssetRow = AssetDetailRow

	export interface BuildingBody {
		code: string
		name: string
		managerCode?: string
		area?: string
		address?: string
		description?: string
	}

	export interface BuildingResponse {
		id: number
		createdAt: string
		updatedAt: string
		code: string
		name: string
		managerCode: string | null
		area: string | null
		address: string | null
		description: string | null
	}

	export interface BuildingTree {
		code: string
		name: string
		managerCode?: string | null
		area?: string | null
		address?: string | null
		description?: string | null
		id: number
		createdAt: string
		updatedAt: string
		floors: {
			buildingId: number
			code?: string | null
			floorNumber: number
			name: string
			description?: string | null
			id: number
			createdAt: string
			updatedAt: string
			rooms: {
				floorId: number
				roomCode: string
				roomName: string
				roomType?: string | null
				manager?: string | null
				managerCode?: string | null
				/**
				 * Hash — không expose ra API
				 */
				accountPassword?: string | null

				capacity?: number | null
				status?: string | null
				description?: string | null
				classId?: number | null
				/**
				 * Client-only flag sau khi strip hash
				 */
				hasAccountPassword?: boolean

				id: number
				createdAt: string
				updatedAt: string
				/**
				 * Tổng số lượng vật tư trong phòng (tổng SL các dòng VT)
				 */
				totalQuantity?: number
			}[]
		}[]
	}

	export interface CatalogAuditLogResponse {
		id: number
		createdAt: string
		action: string
		entityType: string
		actorUserId: number | null
		actorUsername: string | null
		actorDisplayName: string | null
		actorIsAdmin: boolean
		entityId: number | null
		entityCode: string | null
		entityName: string | null
		parentCode: string | null
		parentName: string | null
		summary: string
		details: string | null
	}

	export interface CatalogCategory {
		id: number
		code: string
		name: string
		description: string | null
		/**
		 * true nếu mã ngắn (≤4) = ngành quản lý
		 */
		isNganh: boolean

		/**
		 * Mã ngành cha suy từ mã chuyên ngành (HC2A01 → HC2A)
		 */
		nganhCode: string | null

		/**
		 * Số chuyên ngành (nếu là ngành) hoặc số VT danh mục (nếu là CN)
		 */
		childCount: number

		/**
		 * Tổng SL thực tế từ room_assets thuộc nhánh này
		 */
		stockQuantity: number
	}

	export interface CatalogMaterial {
		id: number
		code: string
		name: string
		unit: string
		categoryId: number
		categoryCode: string
		categoryName: string
		/**
		 * Mã ngành (HC2A…)
		 */
		nganhCode: string

		/**
		 * Tổng SL đang có trên các phòng (room_assets)
		 */
		stockQuantity: number

		/**
		 * SL danh mục (materials.quantity) — tăng/giảm user ngành
		 */
		catalogQuantity: number

		manufactureYear: number | null
		usageYear: number | null
		classification: string | null
		assetStatus: string
		purchaseDate: string | null
		expiryDate: string | null
	}

	export interface CatalogStockLogResponse {
		id: number
		createdAt: string
		movementType: string
		executedAt: string
		materialId: number | null
		materialCode: string | null
		materialName: string
		nganhCode: string
		chuyenNganhCode: string | null
		chuyenNganhName: string | null
		quantity: number
		quantityBefore: number
		quantityAfter: number
		unit: string | null
		isNewMaterial: boolean
		reason: string | null
		note: string | null
		actorUserId: number | null
		actorUsername: string | null
		actorDisplayName: string | null
		actorIsAdmin: boolean
	}

	export interface DeleteBuildingsBody {
		ids: number[]
	}

	export interface DeleteFloorsBody {
		ids: number[]
	}

	export interface DeleteRoomsBody {
		ids: number[]
	}

	export interface ExpiringAssetRow {
		id: number
		code: string | null
		name: string
		category: string
		quantity: number
		unit: string | null
		grade: number
		status: string
		purchaseDate: string | null
		expiryDate: string | null
		description: string | null
		brokenAt: string | null
		repairStartedAt: string | null
		repairCompletedAt: string | null
		repairPerformer: string | null
		/**
		 * true if repairCompletedAt is set
		 */
		repairCompleted: boolean

		roomId: number
		roomCode: string
		roomName: string
		floorId: number
		floorName: string
		buildingId: number
		buildingCode: string
		buildingName: string
		daysUntilExpiry: number
	}

	export interface FloorBody {
		buildingId: number
		code?: string | null
		floorNumber: number
		name: string
		description?: string | null
	}

	export interface FloorResponse {
		id: number
		createdAt: string
		updatedAt: string
		buildingId: number
		code: string | null
		floorNumber: number
		name: string
		description: string | null
	}

	export interface GetAccountAuditLogsQuery {
		q?: string
		limit?: number
	}

	export interface GetCatalogAuditLogsQuery {
		q?: string
		entityType?: string
		limit?: number
	}

	export interface GetFloorsQuery {
		buildingId?: number
	}

	export interface GetRoomAssetsQuery {
		roomId?: number
		status?: string
		category?: string
		code?: string
		codePrefix?: string
		grade?: number
	}

	export interface GetRoomImagesQuery {
		roomId?: number
	}

	export interface GetRoomsQuery {
		floorId?: number
		buildingId?: number
		status?: string
	}

	export interface InventoryLogResponse {
		id: number
		createdAt: string
		updatedAt: string
		roomAssetId: number
		inventoryDate: string
		actualQuantity: number
		expectedQuantity: number
		result: string | null
		note: string | null
	}

	/**
	 * Dòng VT thanh lý (phục vụ màn Thanh lý + báo cáo theo năm)
	 */
	export interface LiquidationAssetRow {
		proposalId: number
		proposalTitle: string
		proposalStatus: string
		proposedAt: string
		unitName: string | null
		nganhCode: string | null
		proposedByDisplayName: string | null
		itemId: number
		materialId: number | null
		materialCode: string | null
		materialName: string
		roomAssetId: number | null
		quantity: number
		unit: string | null
		category: string | null
		fromRoomCode: string | null
		fromRoomName: string | null
		locationNote: string | null
		decisionNumber: string | null
		decisionNganhCode: string | null
		decisionIssuingLevel: string | null
		decisionSigner: string | null
		decisionAt: string | null
		completedAt: string | null
	}

	export interface ListQuery {
		roomAssetId?: number
		roomId?: number
		buildingId?: number
		movementType?: string
		fromDate?: string
		toDate?: string
	}

	export interface ListQuery {
		roomAssetId?: number
		roomId?: number
		buildingId?: number
		movementType?: string
		fromDate?: string
		toDate?: string
	}

	export interface LogListQuery {
		roomAssetId?: number
		roomId?: number
	}

	export interface LogListQuery {
		roomAssetId?: number
		roomId?: number
	}

	export interface LogListQuery {
		roomAssetId?: number
		roomId?: number
	}

	export interface PendingRoomAccountItem {
		userId: number
		username: string
		displayName: string
		status: string | null
		roomId: number | null
		roomCode: string | null
		roomName: string | null
	}

	export interface ProposalItemResponse {
		id: number
		materialId: number | null
		materialCode: string | null
		materialName: string
		roomAssetId: number | null
		sourceAssetId: number | null
		originalGrade: number | null
		originalCode: string | null
		quantity: number
		unit: string | null
		category: string | null
		nganhCode: string | null
		chuyenNganhCode: string | null
		note: string | null
		fromRoomId: number | null
		fromRoomCode: string | null
		fromRoomName: string | null
		locationNote: string | null
		targetRoomId: number | null
		targetRoomCode: string | null
		targetRoomName: string | null
	}

	export interface ProposalLogResponse {
		id: number
		createdAt: string
		proposalId: number | null
		action: string
		proposalType: string | null
		summary: string
		details: string | null
		/**
		 * Đơn vị đề xuất (từ phiếu đề xuất)
		 */
		unitName: string | null

		/**
		 * Ngành đề xuất (từ phiếu)
		 */
		nganhCode: string | null

		actorUserId: number | null
		actorUsername: string | null
		actorDisplayName: string | null
		actorIsAdmin: boolean
	}

	export interface ProposalResponse {
		id: number
		createdAt: string
		updatedAt: string
		proposalType: string
		status: string
		title: string
		description: string | null
		unitId: number | null
		unitName: string | null
		nganhCode: string | null
		proposedByUserId: number | null
		proposedByUsername: string | null
		proposedByDisplayName: string | null
		adminNote: string | null
		decisionNumber: string | null
		decisionNganhCode: string | null
		decisionIssuingLevel: string | null
		decisionSigner: string | null
		decisionAt: string | null
		decidedByUserId: number | null
		decidedByUsername: string | null
		decidedByDisplayName: string | null
		completedAt: string | null
		items: ProposalItemResponse[]
	}

	export interface RepairHistoryRow {
		id: number
		repairDate: string
		content: string
		cost: number
		performer: string | null
		note: string | null
		roomAssetId: number
		assetName: string
		assetCategory: string
		assetStatus: string
		roomId: number
		roomCode: string
		roomName: string
		floorId: number
		floorName: string
		buildingId: number
		buildingCode: string
		buildingName: string
	}

	export interface RepairLogResponse {
		id: number
		createdAt: string
		updatedAt: string
		roomAssetId: number
		repairDate: string
		content: string
		cost: number
		performer: string | null
		note: string | null
	}

	export interface RepairRequestResponse {
		id: number
		createdAt: string
		updatedAt: string
		roomId: number
		roomCode: string | null
		roomName: string | null
		floorName: string | null
		buildingCode: string | null
		buildingName: string | null
		roomAssetId: number | null
		sourceAssetId: number | null
		/**
		 * Số lượng hỏng trên phiếu
		 */
		quantity: number

		assetName: string
		category: string | null
		description: string | null
		status: string
		brokenAt: string
		reportedByName: string
		reportedByUserId: number | null
		assignedToName: string | null
		assignedAt: string | null
		assignedByName: string | null
		repairStartedAt: string | null
		completedAt: string | null
		adminNote: string | null
	}

	export interface ReplacementLogResponse {
		id: number
		createdAt: string
		updatedAt: string
		roomAssetId: number
		replacementDate: string
		oldAsset: string
		newAsset: string
		reason: string | null
		performer: string | null
		note: string | null
	}

	/**
	 * Shared location filters for asset reports
	 */
	export interface ReportFilters {
		buildingId?: number
		floorId?: number
		roomId?: number
		category?: string
	}

	export interface RoomAssetBody {
		roomId: number
		code: string
		name: string
		category: string
		quantity?: number
		unit?: string
		holdingUnitId?: number
		grade?: number
		manufactureYear?: number
		usageYear?: number
		installAddress?: string
		status?: string
		purchaseDate?: string
		expiryDate?: string
		brokenAt?: string
		repairStartedAt?: string
		repairCompletedAt?: string
		repairPerformer?: string
		description?: string
	}

	export interface RoomAssetResponse {
		id: number
		createdAt: string
		updatedAt: string
		roomId: number
		code: string | null
		name: string
		category: string
		/**
		 * SL đang dùng được
		 */
		quantity: number

		/**
		 * SL đang hỏng / chờ-đang sửa (cùng mã VT)
		 */
		brokenQuantity: number

		unit: string | null
		/**
		 * Đơn vị giữ / sử dụng; null = có thể tính vào kho (chưa sử dụng)
		 */
		holdingUnitId: number | null

		grade: number
		manufactureYear: number | null
		usageYear: number | null
		installAddress: string | null
		status: string
		purchaseDate: string | null
		expiryDate: string | null
		brokenAt: string | null
		repairStartedAt: string | null
		repairCompletedAt: string | null
		repairPerformer: string | null
		description: string | null
	}

	export interface RoomBody {
		floorId: number
		roomCode: string
		roomName: string
		roomType?: string
		manager?: string
		managerCode?: string
		/**
		 * Plain password — server hash; mặc định 123456 nếu có tài khoản
		 */
		accountPassword?: string

		capacity?: number
		status?: string
		description?: string
		classId?: number | null
	}

	export interface RoomImageBody {
		roomId: number
		imageUrl: string
		title?: string
		description?: string
	}

	export interface RoomImageResponse {
		id: number
		createdAt: string
		updatedAt: string
		roomId: number
		imageUrl: string
		title: string | null
		description: string | null
	}

	export interface RoomProfile {
		floorId: number
		roomCode: string
		roomName: string
		roomType?: string | null
		manager?: string | null
		managerCode?: string | null
		/**
		 * Hash — không expose ra API
		 */
		accountPassword?: string | null

		capacity?: number | null
		status?: string | null
		description?: string | null
		classId?: number | null
		/**
		 * Client-only flag sau khi strip hash
		 */
		hasAccountPassword?: boolean

		id: number
		createdAt: string
		updatedAt: string
		floor?: {
			buildingId: number
			code?: string | null
			floorNumber: number
			name: string
			description?: string | null
			id: number
			createdAt: string
			updatedAt: string
			building?: schema.BuildingDB
		}
		assets: schema.RoomAssetDB[]
		images: schema.RoomImageDB[]
		repairs: schema.RepairLogDB[]
		inventories: schema.InventoryLogDB[]
		replacements: schema.ReplacementLogDB[]
		movements: schema.AssetMovementLogDB[]
	}

	export interface RoomResponse {
		id: number
		createdAt: string
		updatedAt: string
		floorId: number
		roomCode: string
		roomName: string
		roomType: string | null
		manager: string | null
		managerCode: string | null
		/**
		 * Có mật khẩu tài khoản (không trả hash)
		 */
		hasAccountPassword: boolean

		capacity: number
		status: string
		description: string | null
		/**
		 * Lớp gắn phòng dạy (null = chưa gán)
		 */
		classId: number | null
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.AssignRepairRequest = this.AssignRepairRequest.bind(this)
			this.AssignUserNganh = this.AssignUserNganh.bind(this)
			this.CancelRepairRequest = this.CancelRepairRequest.bind(this)
			this.CompleteRepairRequest = this.CompleteRepairRequest.bind(this)
			this.CreateAssetMovement = this.CreateAssetMovement.bind(this)
			this.CreateAssetProposal = this.CreateAssetProposal.bind(this)
			this.CreateBuilding = this.CreateBuilding.bind(this)
			this.CreateCatalogChuyenNganh =
				this.CreateCatalogChuyenNganh.bind(this)
			this.CreateCatalogMaterial = this.CreateCatalogMaterial.bind(this)
			this.CreateCatalogNganh = this.CreateCatalogNganh.bind(this)
			this.CreateCatalogStockMovement =
				this.CreateCatalogStockMovement.bind(this)
			this.CreateFloor = this.CreateFloor.bind(this)
			this.CreateInventoryLog = this.CreateInventoryLog.bind(this)
			this.CreateRepairLog = this.CreateRepairLog.bind(this)
			this.CreateRepairRequest = this.CreateRepairRequest.bind(this)
			this.CreateReplacementLog = this.CreateReplacementLog.bind(this)
			this.CreateRoom = this.CreateRoom.bind(this)
			this.CreateRoomAsset = this.CreateRoomAsset.bind(this)
			this.CreateRoomImage = this.CreateRoomImage.bind(this)
			this.CreateTransferRecall = this.CreateTransferRecall.bind(this)
			this.DecideAssetProposal = this.DecideAssetProposal.bind(this)
			this.DeleteBuildings = this.DeleteBuildings.bind(this)
			this.DeleteCatalogCategories =
				this.DeleteCatalogCategories.bind(this)
			this.DeleteCatalogMaterials = this.DeleteCatalogMaterials.bind(this)
			this.DeleteFloors = this.DeleteFloors.bind(this)
			this.DeleteInventoryLogs = this.DeleteInventoryLogs.bind(this)
			this.DeleteRepairLogs = this.DeleteRepairLogs.bind(this)
			this.DeleteReplacementLogs = this.DeleteReplacementLogs.bind(this)
			this.DeleteRoomAssets = this.DeleteRoomAssets.bind(this)
			this.DeleteRoomImages = this.DeleteRoomImages.bind(this)
			this.DeleteRooms = this.DeleteRooms.bind(this)
			this.GetAccountAuditLogs = this.GetAccountAuditLogs.bind(this)
			this.GetAssetCatalog = this.GetAssetCatalog.bind(this)
			this.GetAssetCatalogCounts = this.GetAssetCatalogCounts.bind(this)
			this.GetAssetMovementLog = this.GetAssetMovementLog.bind(this)
			this.GetAssetMovementLogs = this.GetAssetMovementLogs.bind(this)
			this.GetAssetMovementReport = this.GetAssetMovementReport.bind(this)
			this.GetAssetProposal = this.GetAssetProposal.bind(this)
			this.GetAssetStatsReport = this.GetAssetStatsReport.bind(this)
			this.GetBrokenAssetsReport = this.GetBrokenAssetsReport.bind(this)
			this.GetBuilding = this.GetBuilding.bind(this)
			this.GetBuildingTree = this.GetBuildingTree.bind(this)
			this.GetBuildings = this.GetBuildings.bind(this)
			this.GetCatalogAuditLogs = this.GetCatalogAuditLogs.bind(this)
			this.GetExpiringAssetsReport =
				this.GetExpiringAssetsReport.bind(this)
			this.GetFloor = this.GetFloor.bind(this)
			this.GetFloors = this.GetFloors.bind(this)
			this.GetInventoryLog = this.GetInventoryLog.bind(this)
			this.GetInventoryLogs = this.GetInventoryLogs.bind(this)
			this.GetMyNganh = this.GetMyNganh.bind(this)
			this.GetPendingProposalCount =
				this.GetPendingProposalCount.bind(this)
			this.GetPendingRoomAccountUsers =
				this.GetPendingRoomAccountUsers.bind(this)
			this.GetRepairHistoryReport = this.GetRepairHistoryReport.bind(this)
			this.GetRepairLog = this.GetRepairLog.bind(this)
			this.GetRepairLogs = this.GetRepairLogs.bind(this)
			this.GetRepairRequest = this.GetRepairRequest.bind(this)
			this.GetRepairRequests = this.GetRepairRequests.bind(this)
			this.GetReplacementLog = this.GetReplacementLog.bind(this)
			this.GetReplacementLogs = this.GetReplacementLogs.bind(this)
			this.GetRoom = this.GetRoom.bind(this)
			this.GetRoomAsset = this.GetRoomAsset.bind(this)
			this.GetRoomAssets = this.GetRoomAssets.bind(this)
			this.GetRoomImage = this.GetRoomImage.bind(this)
			this.GetRoomImages = this.GetRoomImages.bind(this)
			this.GetRoomProfile = this.GetRoomProfile.bind(this)
			this.GetRooms = this.GetRooms.bind(this)
			this.GetUserNganh = this.GetUserNganh.bind(this)
			this.GetWarehouseRoom = this.GetWarehouseRoom.bind(this)
			this.ListAssetBrokenLogs = this.ListAssetBrokenLogs.bind(this)
			this.ListAssetProposalLogs = this.ListAssetProposalLogs.bind(this)
			this.ListAssetProposals = this.ListAssetProposals.bind(this)
			this.ListCatalogStockLogs = this.ListCatalogStockLogs.bind(this)
			this.ListLiquidationAssets = this.ListLiquidationAssets.bind(this)
			this.ResetRoomAccount = this.ResetRoomAccount.bind(this)
			this.SuggestNextChuyenNganhCode =
				this.SuggestNextChuyenNganhCode.bind(this)
			this.SuggestNextMaterialCode =
				this.SuggestNextMaterialCode.bind(this)
			this.SuggestNextNganhCode = this.SuggestNextNganhCode.bind(this)
			this.SyncAllAccounts = this.SyncAllAccounts.bind(this)
			this.UpdateBuilding = this.UpdateBuilding.bind(this)
			this.UpdateCatalogCategory = this.UpdateCatalogCategory.bind(this)
			this.UpdateCatalogMaterial = this.UpdateCatalogMaterial.bind(this)
			this.UpdateFloor = this.UpdateFloor.bind(this)
			this.UpdateInventoryLog = this.UpdateInventoryLog.bind(this)
			this.UpdateRepairLog = this.UpdateRepairLog.bind(this)
			this.UpdateReplacementLog = this.UpdateReplacementLog.bind(this)
			this.UpdateRoom = this.UpdateRoom.bind(this)
			this.UpdateRoomAsset = this.UpdateRoomAsset.bind(this)
			this.UpdateRoomImage = this.UpdateRoomImage.bind(this)
		}

		/**
		 * Admin phân công người sửa.
		 * PATCH /repair-requests/:id/assign
		 */
		public async AssignRepairRequest(
			id: number,
			params: {
				assignedToName: string
				repairStartedAt?: string
				adminNote?: string
				startRepair?: boolean
			}
		): Promise<{
			data: RepairRequestResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/repair-requests/${encodeURIComponent(id)}/assign`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RepairRequestResponse
			}
		}

		/**
		 * POST /asset-catalog/user-nganh — admin gán ngành cho user
		 */
		public async AssignUserNganh(params: {
			userId: number
			nganhCodes: string[]
		}): Promise<{
			data: {
				userId: number
				nganhCodes: string[]
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/asset-catalog/user-nganh`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: {
					userId: number
					nganhCodes: string[]
				}
			}
		}

		/**
		 * Hủy phiếu — trả SL về dòng nguồn (cùng phân cấp cũ), xóa dòng -HONG- nếu có.
		 * PATCH /repair-requests/:id/cancel
		 */
		public async CancelRepairRequest(
			id: number,
			params: {
				adminNote?: string
			}
		): Promise<{
			data: RepairRequestResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/repair-requests/${encodeURIComponent(id)}/cancel`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RepairRequestResponse
			}
		}

		/**
		 * Đánh dấu hoàn thành sửa chữa (phiếu phân công).
		 *
		 * Cấp 5 (hỏng) → luôn về cấp 2 + mã gốc, status NORMAL (bảng VT ổn định).
		 * Không phụ thuộc cấp ban đầu — sửa xong = cấp 2.
		 *
		 * PATCH /repair-requests/:id/complete
		 */
		public async CompleteRepairRequest(
			id: number,
			params: {
				completedAt?: string
				adminNote?: string
			}
		): Promise<{
			data: RepairRequestResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/repair-requests/${encodeURIComponent(id)}/complete`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RepairRequestResponse
			}
		}

		/**
		 * Nhập tăng/giảm hoặc điều chỉnh số liệu vật tư
		 */
		public async CreateAssetMovement(
			id: number,
			params: {
				movementType: schema.AssetMovementType
				executedAt: string
				executingUnit?: string
				installAddress?: string
				assetName?: string
				quantity: number
				grade?: number
				manufactureYear?: number
				usageYear?: number
				reasonCode?: string
				reasonOther?: string
				decisionDate?: string
				decisionNumber?: string
				signer?: string
				performer?: string
				explanation?: string
				note?: string
				/**
				 * Đơn vị quản lý — cập nhật room_assets.holding_unit_id
				 */
				holdingUnitId?: number | null
			}
		): Promise<{
			data: AssetMovementResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/room-assets/${encodeURIComponent(id)}/movements`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: AssetMovementResponse
			}
		}

		/**
		 * POST /asset-proposals — đơn vị tạo đề xuất
		 */
		public async CreateAssetProposal(params: {
			proposalType: 'REPAIR' | 'RECALL' | 'LIQUIDATION'
			title: string
			description?: string
			unitId?: number
			unitName?: string
			nganhCode?: string
			items: {
				materialId?: number
				materialCode?: string
				materialName: string
				roomAssetId?: number
				quantity: number
				unit?: string
				category?: string
				nganhCode?: string
				chuyenNganhCode?: string
				note?: string
				fromRoomId?: number
				fromRoomCode?: string
				fromRoomName?: string
				locationNote?: string
				targetRoomId?: number
				targetRoomCode?: string
				targetRoomName?: string
			}[]
		}): Promise<{
			data: ProposalResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/asset-proposals`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ProposalResponse
			}
		}

		public async CreateBuilding(params: BuildingBody): Promise<{
			data: BuildingResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/buildings`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: BuildingResponse
			}
		}

		/**
		 * Thêm chuyên ngành dưới ngành. Bắt buộc tên + nganhCode; mã HC2x0y tự sinh.
		 * POST /asset-catalog/chuyen-nganh
		 */
		public async CreateCatalogChuyenNganh(params: {
			nganhCode: string
			name: string
			description?: string
		}): Promise<{
			data: CatalogCategory
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/asset-catalog/chuyen-nganh`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: CatalogCategory
			}
		}

		/**
		 * Thêm thiết bị mới vào danh mục (dưới chuyên ngành).
		 * Mã tự sinh theo ngành → chuyên ngành → seq (HC2A0101…).
		 * POST /asset-catalog/materials
		 */
		public async CreateCatalogMaterial(params: {
			chuyenNganhCode: string
			name: string
			unit?: string
			quantity?: number
			/**
			 * Tùy chọn — nếu trống hệ thống tự sinh
			 */
			code?: string

			description?: string
			manufactureYear?: number
			usageYear?: number
			classification?: string
			assetStatus?: string
			purchaseDate?: string
			expiryDate?: string
		}): Promise<{
			data: CatalogMaterial
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/asset-catalog/materials`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: CatalogMaterial
			}
		}

		/**
		 * Thêm ngành mới. Bắt buộc tên; mã hệ thống xin theo HC2x (x = A–Z).
		 * POST /asset-catalog/nganh
		 */
		public async CreateCatalogNganh(params: {
			name: string
			description?: string
		}): Promise<{
			data: CatalogCategory
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/asset-catalog/nganh`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: CatalogCategory
			}
		}

		/**
		 * POST /asset-catalog/stock-movements
		 * User ngành: tăng/giảm SL danh mục. Tên mới → sinh mã theo loại vật (chuyên ngành).
		 */
		public async CreateCatalogStockMovement(params: {
			movementType: 'INCREASE' | 'DECREASE'
			/**
			 * Ngành HC2A
			 */
			nganhCode: string

			/**
			 * Loại vật / chuyên ngành HC2A01 — hoặc tên mới để sinh loại
			 */
			chuyenNganhCode?: string

			/**
			 * Tên loại vật nếu tạo mới (khi không có mã loại)
			 */
			chuyenNganhName?: string

			/**
			 * Mã VT danh mục nếu đã có
			 */
			materialCode?: string

			/**
			 * Tên VT (bắt buộc nếu tạo mới / tìm theo tên)
			 */
			materialName: string

			quantity: number
			unit?: string
			reason?: string
			note?: string
			executedAt?: string
		}): Promise<{
			data: {
				material: {
					id: number
					code: string
					name: string
					quantity: number
					unit: string
					chuyenNganhCode: string
					nganhCode: string
					isNew: boolean
				}
				log: CatalogStockLogResponse
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/asset-catalog/stock-movements`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: {
					material: {
						id: number
						code: string
						name: string
						quantity: number
						unit: string
						chuyenNganhCode: string
						nganhCode: string
						isNew: boolean
					}
					log: CatalogStockLogResponse
				}
			}
		}

		public async CreateFloor(params: FloorBody): Promise<{
			data: FloorResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/floors`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: FloorResponse
			}
		}

		public async CreateInventoryLog(params: {
			roomAssetId: number
			inventoryDate: string
			actualQuantity: number
			expectedQuantity?: number
			result?: string
			note?: string
		}): Promise<{
			data: InventoryLogResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/inventory-logs`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: InventoryLogResponse
			}
		}

		public async CreateRepairLog(params: {
			roomAssetId: number
			repairDate: string
			content: string
			cost?: number
			performer?: string
			note?: string
		}): Promise<{
			data: RepairLogResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/repair-logs`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RepairLogResponse
			}
		}

		/**
		 * Phòng báo hỏng thiết bị (cấp phòng).
		 * POST /repair-requests
		 */
		public async CreateRepairRequest(params: {
			roomId: number
			roomAssetId?: number
			/**
			 * Số lượng hỏng — bắt buộc khi chọn từ danh mục (mặc định 1)
			 */
			quantity?: number

			assetName: string
			category?: string
			description?: string
			brokenAt?: string
			reportedByName?: string
		}): Promise<{
			data: RepairRequestResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/repair-requests`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RepairRequestResponse
			}
		}

		public async CreateReplacementLog(params: {
			roomAssetId: number
			replacementDate: string
			oldAsset: string
			newAsset: string
			reason?: string
			performer?: string
			note?: string
		}): Promise<{
			data: ReplacementLogResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/replacement-logs`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ReplacementLogResponse
			}
		}

		public async CreateRoom(params: RoomBody): Promise<{
			data: RoomResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/rooms`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RoomResponse
			}
		}

		public async CreateRoomAsset(params: RoomAssetBody): Promise<{
			data: RoomAssetResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/room-assets`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RoomAssetResponse
			}
		}

		public async CreateRoomImage(params: RoomImageBody): Promise<{
			data: RoomImageResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/room-images`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RoomImageResponse
			}
		}

		/**
		 * Điều động (TRANSFER) hoặc thu hồi (RECALL) vật tư sang phòng đích.
		 * Ghi nhật ký movementType = TRANSFER | RECALL.
		 */
		public async CreateTransferRecall(
			id: number,
			params: {
				movementType: 'TRANSFER' | 'RECALL'
				/**
				 * RECALL: optional → kho KHO-VT
				 */
				targetRoomId?: number

				quantity: number
				executedAt: string
				holdingUnitId?: number | null
				executingUnit?: string
				installAddress?: string
				decisionDate?: string
				decisionNumber?: string
				signer?: string
				performer?: string
				reasonOther?: string
				note?: string
			}
		): Promise<{
			data: AssetMovementResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/room-assets/${encodeURIComponent(id)}/transfer-recall`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: AssetMovementResponse
			}
		}

		/**
		 * POST /asset-proposals/:id/decide
		 * Admin duyệt / từ chối / hoàn thành.
		 * - RECALL COMPLETED: thu hồi VT về kho (giảm nguồn, tăng kho).
		 * - LIQUIDATION COMPLETED: nhập QĐ → giảm danh mục + room_assets lý do «Thanh lý».
		 * - REPAIR COMPLETED: ghi kết quả sửa chữa (adminNote).
		 */
		public async DecideAssetProposal(
			id: number,
			params: {
				/**
				 * APPROVED | REJECTED | COMPLETED
				 */
				decision: 'APPROVED' | 'REJECTED' | 'COMPLETED'

				adminNote?: string
				/**
				 * Bắt buộc khi COMPLETED + LIQUIDATION
				 */
				decisionNumber?: string

				decisionNganhCode?: string
				decisionIssuingLevel?: string
				decisionSigner?: string
				decisionAt?: string
				/**
				 * Kho đích khi COMPLETED + RECALL (áp dụng mọi item chưa có target)
				 */
				targetRoomId?: number
			}
		): Promise<{
			data: ProposalResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/asset-proposals/${encodeURIComponent(id)}/decide`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ProposalResponse
			}
		}

		public async DeleteBuildings(params: DeleteBuildingsBody): Promise<{
			ids: number[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/buildings/delete`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ids: number[]
			}
		}

		/**
		 * Xóa ngành hoặc loại vật.
		 * - Ngành: chặn nếu còn loại vật con.
		 * - Loại vật: chặn nếu còn vật tư danh mục.
		 * POST /asset-catalog/categories/delete
		 */
		public async DeleteCatalogCategories(params: {
			ids: number[]
		}): Promise<{
			ids: number[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/asset-catalog/categories/delete`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ids: number[]
			}
		}

		/**
		 * Xóa vật tư danh mục.
		 * POST /asset-catalog/materials/delete
		 */
		public async DeleteCatalogMaterials(params: {
			ids: number[]
		}): Promise<{
			ids: number[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/asset-catalog/materials/delete`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ids: number[]
			}
		}

		public async DeleteFloors(params: DeleteFloorsBody): Promise<{
			ids: number[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/floors/delete`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ids: number[]
			}
		}

		public async DeleteInventoryLogs(params: { ids: number[] }): Promise<{
			ids: number[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/inventory-logs/delete`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ids: number[]
			}
		}

		public async DeleteRepairLogs(params: { ids: number[] }): Promise<{
			ids: number[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/repair-logs/delete`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ids: number[]
			}
		}

		public async DeleteReplacementLogs(params: { ids: number[] }): Promise<{
			ids: number[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/replacement-logs/delete`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ids: number[]
			}
		}

		public async DeleteRoomAssets(params: { ids: number[] }): Promise<{
			ids: number[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/room-assets/delete`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ids: number[]
			}
		}

		public async DeleteRoomImages(params: { ids: number[] }): Promise<{
			ids: number[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/room-images/delete`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ids: number[]
			}
		}

		public async DeleteRooms(params: DeleteRoomsBody): Promise<{
			ids: number[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/rooms/delete`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ids: number[]
			}
		}

		public async GetAccountAuditLogs(
			params: GetAccountAuditLogsQuery
		): Promise<{
			data: AccountAuditLogResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				limit:
					params.limit === undefined
						? undefined
						: String(params.limit),
				q: params.q
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/account-audit-logs`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: AccountAuditLogResponse[]
			}
		}

		/**
		 * GET /asset-catalog?nganhCode=HC2A&chuyenNganhCode=HC2A01
		 * Không truyền filter → trả về toàn bộ danh mục + SL thực tế.
		 */
		public async GetAssetCatalog(params: {
			nganhCode?: string
			chuyenNganhCode?: string
		}): Promise<{
			data: AssetCatalogResponse
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				chuyenNganhCode: params.chuyenNganhCode,
				nganhCode: params.nganhCode
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-catalog`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: AssetCatalogResponse
			}
		}

		/**
		 * Đếm nhanh số vật tư danh mục theo ngành / chuyên ngành (debug / UI).
		 * GET /asset-catalog/counts
		 */
		public async GetAssetCatalogCounts(): Promise<{
			data: {
				code: string
				name: string
				kind: string
				count: number
			}[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-catalog/counts`
			)
			return (await resp.json()) as {
				data: {
					code: string
					name: string
					kind: string
					count: number
				}[]
			}
		}

		public async GetAssetMovementLog(id: number): Promise<{
			data: AssetMovementResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-movement-logs/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: AssetMovementResponse
			}
		}

		public async GetAssetMovementLogs(params: ListQuery): Promise<{
			data: AssetMovementResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				buildingId:
					params.buildingId === undefined
						? undefined
						: String(params.buildingId),
				fromDate: params.fromDate,
				movementType: params.movementType,
				roomAssetId:
					params.roomAssetId === undefined
						? undefined
						: String(params.roomAssetId),
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId),
				toDate: params.toDate
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-movement-logs`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: AssetMovementResponse[]
			}
		}

		/**
		 * Báo cáo biến động kèm vị trí (tòa/tầng/phòng)
		 */
		public async GetAssetMovementReport(params: ListQuery): Promise<{
			data: AssetMovementReportResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				buildingId:
					params.buildingId === undefined
						? undefined
						: String(params.buildingId),
				fromDate: params.fromDate,
				movementType: params.movementType,
				roomAssetId:
					params.roomAssetId === undefined
						? undefined
						: String(params.roomAssetId),
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId),
				toDate: params.toDate
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-reports/movements`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: AssetMovementReportResponse[]
			}
		}

		/**
		 * GET /asset-proposals/:id
		 */
		public async GetAssetProposal(id: number): Promise<{
			data: ProposalResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-proposals/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: ProposalResponse
			}
		}

		/**
		 * Thống kê vật tư — totals by status & category, optional location scope.
		 * GET /asset-reports/stats?buildingId=&floorId=&roomId=&category=
		 */
		public async GetAssetStatsReport(params: ReportFilters): Promise<{
			data: AssetStatsReport
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				buildingId:
					params.buildingId === undefined
						? undefined
						: String(params.buildingId),
				category: params.category,
				floorId:
					params.floorId === undefined
						? undefined
						: String(params.floorId),
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-reports/stats`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: AssetStatsReport
			}
		}

		/**
		 * Vật tư hỏng — status BROKEN (optionally include REPAIRING via includeRepairing).
		 * GET /asset-reports/broken?buildingId=&includeRepairing=true
		 */
		public async GetBrokenAssetsReport(params: {
			buildingId?: number
			floorId?: number
			roomId?: number
			category?: string
			includeRepairing?: boolean
		}): Promise<{
			data: BrokenAssetRow[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				buildingId:
					params.buildingId === undefined
						? undefined
						: String(params.buildingId),
				category: params.category,
				floorId:
					params.floorId === undefined
						? undefined
						: String(params.floorId),
				includeRepairing:
					params.includeRepairing === undefined
						? undefined
						: String(params.includeRepairing),
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-reports/broken`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: BrokenAssetRow[]
			}
		}

		public async GetBuilding(id: number): Promise<{
			data: BuildingTree
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/buildings/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: BuildingTree
			}
		}

		public async GetBuildingTree(): Promise<{
			data: BuildingTree[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/buildings/tree`
			)
			return (await resp.json()) as {
				data: BuildingTree[]
			}
		}

		public async GetBuildings(): Promise<{
			data: BuildingResponse[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI('GET', `/buildings`)
			return (await resp.json()) as {
				data: BuildingResponse[]
			}
		}

		public async GetCatalogAuditLogs(
			params: GetCatalogAuditLogsQuery
		): Promise<{
			data: CatalogAuditLogResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				entityType: params.entityType,
				limit:
					params.limit === undefined
						? undefined
						: String(params.limit),
				q: params.q
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/catalog-audit-logs`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: CatalogAuditLogResponse[]
			}
		}

		/**
		 * Vật tư sắp hết hạn — expiryDate within `withinDays` (default 30), from today.
		 * GET /asset-reports/expiring?withinDays=30&buildingId=
		 */
		public async GetExpiringAssetsReport(params: {
			buildingId?: number
			floorId?: number
			roomId?: number
			category?: string
			withinDays?: number
		}): Promise<{
			data: ExpiringAssetRow[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				buildingId:
					params.buildingId === undefined
						? undefined
						: String(params.buildingId),
				category: params.category,
				floorId:
					params.floorId === undefined
						? undefined
						: String(params.floorId),
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId),
				withinDays:
					params.withinDays === undefined
						? undefined
						: String(params.withinDays)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-reports/expiring`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: ExpiringAssetRow[]
			}
		}

		public async GetFloor(id: number): Promise<{
			data: FloorResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/floors/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: FloorResponse
			}
		}

		public async GetFloors(params: GetFloorsQuery): Promise<{
			data: FloorResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				buildingId:
					params.buildingId === undefined
						? undefined
						: String(params.buildingId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/floors`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: FloorResponse[]
			}
		}

		public async GetInventoryLog(id: number): Promise<{
			data: InventoryLogResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/inventory-logs/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: InventoryLogResponse
			}
		}

		public async GetInventoryLogs(params: LogListQuery): Promise<{
			data: InventoryLogResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				roomAssetId:
					params.roomAssetId === undefined
						? undefined
						: String(params.roomAssetId),
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/inventory-logs`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: InventoryLogResponse[]
			}
		}

		/**
		 * GET /asset-catalog/my-nganh — ngành được gán cho user hiện tại
		 */
		public async GetMyNganh(): Promise<{
			data: {
				code: string
				name: string
			}[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-catalog/my-nganh`
			)
			return (await resp.json()) as {
				data: {
					code: string
					name: string
				}[]
			}
		}

		/**
		 * GET /asset-proposals/pending-count — badge BGH (chờ duyệt) / ngành (chờ sửa)
		 */
		public async GetPendingProposalCount(): Promise<{
			data: {
				count: number
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-proposals/pending-count`
			)
			return (await resp.json()) as {
				data: {
					count: number
				}
			}
		}

		public async GetPendingRoomAccountUsers(): Promise<{
			data: {
				count: number
				items: PendingRoomAccountItem[]
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/users/pending-room-accounts`
			)
			return (await resp.json()) as {
				data: {
					count: number
					items: PendingRoomAccountItem[]
				}
			}
		}

		/**
		 * Lịch sử sửa chữa — repair logs joined with asset/room/building.
		 * GET /asset-reports/repairs?buildingId=&roomId=&roomAssetId=&fromDate=&toDate=
		 */
		public async GetRepairHistoryReport(params: {
			buildingId?: number
			floorId?: number
			roomId?: number
			category?: string
			roomAssetId?: number
			fromDate?: string
			toDate?: string
		}): Promise<{
			data: RepairHistoryRow[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				buildingId:
					params.buildingId === undefined
						? undefined
						: String(params.buildingId),
				category: params.category,
				floorId:
					params.floorId === undefined
						? undefined
						: String(params.floorId),
				fromDate: params.fromDate,
				roomAssetId:
					params.roomAssetId === undefined
						? undefined
						: String(params.roomAssetId),
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId),
				toDate: params.toDate
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-reports/repairs`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: RepairHistoryRow[]
			}
		}

		public async GetRepairLog(id: number): Promise<{
			data: RepairLogResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/repair-logs/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: RepairLogResponse
			}
		}

		public async GetRepairLogs(params: LogListQuery): Promise<{
			data: RepairLogResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				roomAssetId:
					params.roomAssetId === undefined
						? undefined
						: String(params.roomAssetId),
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/repair-logs`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: RepairLogResponse[]
			}
		}

		public async GetRepairRequest(id: number): Promise<{
			data: RepairRequestResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/repair-requests/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: RepairRequestResponse
			}
		}

		/**
		 * Danh sách phiếu (admin / phòng).
		 * GET /repair-requests?roomId=&status=
		 */
		public async GetRepairRequests(params: {
			roomId?: number
			status?: string
		}): Promise<{
			data: RepairRequestResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId),
				status: params.status
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/repair-requests`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: RepairRequestResponse[]
			}
		}

		public async GetReplacementLog(id: number): Promise<{
			data: ReplacementLogResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/replacement-logs/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: ReplacementLogResponse
			}
		}

		public async GetReplacementLogs(params: LogListQuery): Promise<{
			data: ReplacementLogResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				roomAssetId:
					params.roomAssetId === undefined
						? undefined
						: String(params.roomAssetId),
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/replacement-logs`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: ReplacementLogResponse[]
			}
		}

		public async GetRoom(id: number): Promise<{
			data: RoomResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/rooms/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: RoomResponse
			}
		}

		public async GetRoomAsset(id: number): Promise<{
			data: RoomAssetResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/room-assets/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: RoomAssetResponse
			}
		}

		public async GetRoomAssets(params: GetRoomAssetsQuery): Promise<{
			data: RoomAssetResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				category: params.category,
				code: params.code,
				codePrefix: params.codePrefix,
				grade:
					params.grade === undefined
						? undefined
						: String(params.grade),
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId),
				status: params.status
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/room-assets`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: RoomAssetResponse[]
			}
		}

		public async GetRoomImage(id: number): Promise<{
			data: RoomImageResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/room-images/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: RoomImageResponse
			}
		}

		public async GetRoomImages(params: GetRoomImagesQuery): Promise<{
			data: RoomImageResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				roomId:
					params.roomId === undefined
						? undefined
						: String(params.roomId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/room-images`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: RoomImageResponse[]
			}
		}

		/**
		 * Full room profile: general info + assets + images + 3 log streams
		 */
		public async GetRoomProfile(id: number): Promise<{
			data: RoomProfile
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/rooms/${encodeURIComponent(id)}/profile`
			)
			return (await resp.json()) as {
				data: RoomProfile
			}
		}

		public async GetRooms(params: GetRoomsQuery): Promise<{
			data: RoomResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				buildingId:
					params.buildingId === undefined
						? undefined
						: String(params.buildingId),
				floorId:
					params.floorId === undefined
						? undefined
						: String(params.floorId),
				status: params.status
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/rooms`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: RoomResponse[]
			}
		}

		/**
		 * GET /asset-catalog/user-nganh?userId= — admin xem ngành đã gán
		 */
		public async GetUserNganh(params: { userId?: number }): Promise<{
			data: {
				userId: number
				nganhCodes: string[]
			}
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				userId:
					params.userId === undefined
						? undefined
						: String(params.userId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-catalog/user-nganh`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: {
					userId: number
					nganhCodes: string[]
				}
			}
		}

		/**
		 * Kho hệ thống KHO-VT (tự tạo nếu chưa có) — VT thu hồi / trả trên
		 */
		public async GetWarehouseRoom(): Promise<{
			data: RoomResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/rooms/warehouse`
			)
			return (await resp.json()) as {
				data: RoomResponse
			}
		}

		/**
		 * GET /asset-broken-logs — nhật ký hỏng/sửa (để tải xuống)
		 */
		public async ListAssetBrokenLogs(params: {
			eventType?: string
			sourceType?: string
			proposalId?: number
			fromDate?: string
			toDate?: string
			limit?: number
		}): Promise<{
			data: AssetBrokenLogResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				eventType: params.eventType,
				fromDate: params.fromDate,
				limit:
					params.limit === undefined
						? undefined
						: String(params.limit),
				proposalId:
					params.proposalId === undefined
						? undefined
						: String(params.proposalId),
				sourceType: params.sourceType,
				toDate: params.toDate
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-broken-logs`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: AssetBrokenLogResponse[]
			}
		}

		/**
		 * GET /asset-proposal-logs
		 */
		public async ListAssetProposalLogs(params: {
			proposalId?: number
			fromDate?: string
			toDate?: string
			limit?: number
		}): Promise<{
			data: ProposalLogResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				fromDate: params.fromDate,
				limit:
					params.limit === undefined
						? undefined
						: String(params.limit),
				proposalId:
					params.proposalId === undefined
						? undefined
						: String(params.proposalId),
				toDate: params.toDate
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-proposal-logs`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: ProposalLogResponse[]
			}
		}

		/**
		 * GET /asset-proposals
		 */
		public async ListAssetProposals(params: {
			status?: string
			proposalType?: string
			mine?: boolean
			limit?: number
		}): Promise<{
			data: ProposalResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				limit:
					params.limit === undefined
						? undefined
						: String(params.limit),
				mine:
					params.mine === undefined ? undefined : String(params.mine),
				proposalType: params.proposalType,
				status: params.status
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-proposals`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: ProposalResponse[]
			}
		}

		/**
		 * GET /asset-catalog/stock-logs — admin / user xem log tăng giảm
		 */
		public async ListCatalogStockLogs(params: {
			nganhCode?: string
			fromDate?: string
			toDate?: string
			limit?: number
		}): Promise<{
			data: CatalogStockLogResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				fromDate: params.fromDate,
				limit:
					params.limit === undefined
						? undefined
						: String(params.limit),
				nganhCode: params.nganhCode,
				toDate: params.toDate
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-catalog/stock-logs`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: CatalogStockLogResponse[]
			}
		}

		/**
		 * GET /asset-proposals/liquidations
		 * Danh sách VT các đơn vị đề nghị thanh lý (+ đã thanh lý) — lọc năm QĐ / trạng thái.
		 */
		public async ListLiquidationAssets(params: {
			status?: string
			year?: number
			limit?: number
		}): Promise<{
			data: LiquidationAssetRow[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				limit:
					params.limit === undefined
						? undefined
						: String(params.limit),
				status: params.status,
				year:
					params.year === undefined ? undefined : String(params.year)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-proposals/liquidations`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: LiquidationAssetRow[]
			}
		}

		/**
		 * Chỉ reset mật khẩu tài khoản phòng về mặc định 123456.
		 * Không đổi mã / tên tài khoản.
		 */
		public async ResetRoomAccount(id: number): Promise<{
			data: RoomResponse
			defaultPassword: string
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/rooms/${encodeURIComponent(id)}/reset-account`
			)
			return (await resp.json()) as {
				data: RoomResponse
				defaultPassword: string
			}
		}

		/**
		 * Gợi ý mã chuyên ngành tiếp theo trong ngành (HC2A01, HC2A02…).
		 * GET /asset-catalog/next-chuyen-nganh-code?nganhCode=HC2A
		 */
		public async SuggestNextChuyenNganhCode(params: {
			nganhCode: string
		}): Promise<{
			data: {
				code: string
				nganhCode: string
			}
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				nganhCode: params.nganhCode
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-catalog/next-chuyen-nganh-code`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: {
					code: string
					nganhCode: string
				}
			}
		}

		/**
		 * Gợi ý mã VT danh mục tiếp theo trong chuyên ngành.
		 * GET /asset-catalog/next-code?chuyenNganhCode=HC2A01
		 */
		public async SuggestNextMaterialCode(params: {
			chuyenNganhCode: string
		}): Promise<{
			data: {
				code: string
				chuyenNganhCode: string
			}
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				chuyenNganhCode: params.chuyenNganhCode
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-catalog/next-code`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: {
					code: string
					chuyenNganhCode: string
				}
			}
		}

		/**
		 * Gợi ý mã ngành tiếp theo (HC2A…HC2Z, gồm J).
		 * GET /asset-catalog/next-nganh-code
		 */
		public async SuggestNextNganhCode(): Promise<{
			data: {
				code: string
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/asset-catalog/next-nganh-code`
			)
			return (await resp.json()) as {
				data: {
					code: string
				}
			}
		}

		/**
		 * Đồng bộ toàn bộ tài khoản:
		 * 1) Phòng có managerCode → users (pending nếu chưa role)
		 * 2) exam_teachers / faculty_heads / teaching_assignments ← username/tên từ users
		 */
		public async SyncAllAccounts(): Promise<{
			data: {
				roomUsersCreated: number
				teachersUpdated: number
				facultyHeadsUpdated: number
				assignmentsUpdated: number
				pendingRoomAccounts: number
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/users/sync-accounts`
			)
			return (await resp.json()) as {
				data: {
					roomUsersCreated: number
					teachersUpdated: number
					facultyHeadsUpdated: number
					assignmentsUpdated: number
					pendingRoomAccounts: number
				}
			}
		}

		public async UpdateBuilding(
			id: number,
			params: {
				code?: string
				name?: string
				managerCode?: string
				area?: string
				address?: string
				description?: string
			}
		): Promise<{
			data: BuildingResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/buildings/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: BuildingResponse
			}
		}

		/**
		 * Sửa tên (và mô tả) ngành / chuyên ngành — không đổi mã.
		 * PATCH /asset-catalog/categories/:id
		 */
		public async UpdateCatalogCategory(
			id: number,
			params: {
				name?: string
				description?: string | null
			}
		): Promise<{
			data: CatalogCategory
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/asset-catalog/categories/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: CatalogCategory
			}
		}

		/**
		 * Sửa vật tư danh mục (tên / ĐVT / mô tả).
		 * PATCH /asset-catalog/materials/:id
		 */
		public async UpdateCatalogMaterial(
			id: number,
			params: {
				name?: string
				unit?: string
				description?: string | null
				manufactureYear?: number | null
				usageYear?: number | null
				classification?: string | null
				assetStatus?: string | null
				purchaseDate?: string | null
				expiryDate?: string | null
			}
		): Promise<{
			data: CatalogMaterial
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/asset-catalog/materials/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: CatalogMaterial
			}
		}

		public async UpdateFloor(
			id: number,
			params: {
				buildingId?: number
				code?: string | null
				floorNumber?: number
				name?: string
				description?: string | null
			}
		): Promise<{
			data: FloorResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/floors/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: FloorResponse
			}
		}

		public async UpdateInventoryLog(
			id: number,
			params: {
				roomAssetId?: number
				inventoryDate?: string
				actualQuantity?: number
				expectedQuantity?: number
				result?: string
				note?: string
			}
		): Promise<{
			data: InventoryLogResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/inventory-logs/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: InventoryLogResponse
			}
		}

		public async UpdateRepairLog(
			id: number,
			params: {
				roomAssetId?: number
				repairDate?: string
				content?: string
				cost?: number
				performer?: string
				note?: string
			}
		): Promise<{
			data: RepairLogResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/repair-logs/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RepairLogResponse
			}
		}

		public async UpdateReplacementLog(
			id: number,
			params: {
				roomAssetId?: number
				replacementDate?: string
				oldAsset?: string
				newAsset?: string
				reason?: string
				performer?: string
				note?: string
			}
		): Promise<{
			data: ReplacementLogResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/replacement-logs/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ReplacementLogResponse
			}
		}

		public async UpdateRoom(
			id: number,
			params: {
				floorId?: number
				roomCode?: string
				roomName?: string
				roomType?: string
				manager?: string
				/**
				 * Mã tài khoản — được phép sửa
				 */
				managerCode?: string

				/**
				 * Plain password mới; bỏ trống = giữ cũ
				 */
				accountPassword?: string

				capacity?: number
				status?: string
				description?: string
				classId?: number | null
			}
		): Promise<{
			data: RoomResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/rooms/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RoomResponse
			}
		}

		public async UpdateRoomAsset(
			id: number,
			params: {
				roomId?: number
				code?: string
				name?: string
				category?: string
				quantity?: number
				unit?: string
				holdingUnitId?: number | null
				grade?: number
				manufactureYear?: number
				usageYear?: number
				installAddress?: string
				status?: string
				purchaseDate?: string
				expiryDate?: string
				brokenAt?: string
				repairStartedAt?: string
				repairCompletedAt?: string
				repairPerformer?: string
				description?: string
			}
		): Promise<{
			data: RoomAssetResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/room-assets/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RoomAssetResponse
			}
		}

		public async UpdateRoomImage(
			id: number,
			params: {
				roomId?: number
				imageUrl?: string
				title?: string
				description?: string
			}
		): Promise<{
			data: RoomImageResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/room-images/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RoomImageResponse
			}
		}
	}
}

export namespace auth {
	export interface AuthParams {
		authorization: string
	}

	export interface ChangeUserPasswordRequest {
		prevPassword: string
		password: string
	}

	export interface GetUserInfoResponse {
		data: users.User
	}

	export interface LoginRequest {
		username: string
		password: string
	}

	export interface LoginResponse {
		accessToken: string
		refreshToken: string
	}

	export interface RefreshTokenRequest {
		token: string
	}

	export interface RefreshTokenResponse {
		accessToken: string
		refreshToken: string
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.ChangeUserPassword = this.ChangeUserPassword.bind(this)
			this.GetUserInfo = this.GetUserInfo.bind(this)
			this.Login = this.Login.bind(this)
			this.RefreshToken = this.RefreshToken.bind(this)
			this.UpdateMyProfile = this.UpdateMyProfile.bind(this)
			this.UpdateMySignature = this.UpdateMySignature.bind(this)
		}

		public async ChangeUserPassword(
			params: ChangeUserPasswordRequest
		): Promise<void> {
			await this.baseClient.callTypedAPI(
				'PATCH',
				`/authn/change-pwd`,
				JSON.stringify(params)
			)
		}

		public async GetUserInfo(): Promise<GetUserInfoResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI('GET', `/authn/me`)
			return (await resp.json()) as GetUserInfoResponse
		}

		public async Login(params: LoginRequest): Promise<LoginResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/authn/login`,
				JSON.stringify(params)
			)
			return (await resp.json()) as LoginResponse
		}

		public async RefreshToken(
			params: RefreshTokenRequest
		): Promise<RefreshTokenResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/authn/refresh`,
				JSON.stringify(params)
			)
			return (await resp.json()) as RefreshTokenResponse
		}

		/**
		 * Cập nhật hồ sơ chính mình (họ tên, cấp bậc) — không cần users:update.
		 * Chức vụ gắn loại tài khoản khi tạo — không cho đổi tại đây.
		 */
		public async UpdateMyProfile(params: {
			displayName?: string
			rank?: string
		}): Promise<{
			ok: boolean
			displayName: string
			rank: string | null
			position: string | null
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/authn/my-profile`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ok: boolean
				displayName: string
				rank: string | null
				position: string | null
			}
		}

		/**
		 * Upload chữ ký số của chính mình (CNK / BGH) — không cần users:update.
		 * body.signatureUrl: data URL hoặc URL ảnh.
		 */
		public async UpdateMySignature(params: {
			signatureUrl: string
		}): Promise<{
			ok: boolean
			signatureUrl: string
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/authn/my-signature`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ok: boolean
				signatureUrl: string
			}
		}
	}
}

export namespace classes {
	export interface BulkClassResponse {
		data: ClassResponse[]
	}

	export interface Class {
		id: number
		createdAt: string
		updatedAt: string
		name: string
		description: string
		graduatedAt: string | null
		status: 'ongoing' | 'graduated'
		unit: units.UnitDB
	}

	export interface ClassBody {
		name: string
		description?: string
		graduatedAt?: string
		unitId: number
	}

	export interface ClassResponse {
		id: number
		createdAt: string
		updatedAt: string
		name: string
		description: string
		graduatedAt: string | null
		status: 'ongoing' | 'graduated'
		unitId: number
	}

	export interface DeleteClassRequest {
		ids: number[]
	}

	export interface DeleteClassResponse {
		ids: number[]
	}

	export interface GetClassByIdResponse {
		data?: Class
	}

	export interface GetClassesRequest {
		ids?: number[]
		unitIds?: number[]
	}

	export interface GetClassesResponse {
		data: ClassResponse[]
	}

	export interface ImportMoodleClassesBody {
		unitId: number
		/**
		 * id course Moodle
		 */
		courseIds: number[]
	}

	export interface ImportMoodleClassesResponse {
		data: ClassResponse[]
		imported: number
		skipped: number
	}

	export interface ListMoodleCoursesRequest {
		/**
		 * Đại đội sẽ gán lớp khi import — để đánh dấu đã import
		 */
		unitId?: number
	}

	export interface ListMoodleCoursesResponse {
		data: MoodleCourseItem[]
		/**
		 * false nếu không kết nối / tắt sync
		 */
		connected: boolean

		message?: string
	}

	/**
	 * Khóa Moodle (course) — học chung khóa, import thành lớp local
	 */
	export interface MoodleCourseItem {
		id: number
		fullname: string
		shortname: string
		/**
		 * Đã có lớp local (cùng unit) map từ course này
		 */
		alreadyImported: boolean
	}

	export interface UpdateClassBody {
		data: UpdatePayload[]
	}

	export interface UpdatePayload {
		id: number
		name?: string
		description?: string
		graduatedAt?: string
		unitId?: number
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.CreateClass = this.CreateClass.bind(this)
			this.DeleteClasss = this.DeleteClasss.bind(this)
			this.GetClassById = this.GetClassById.bind(this)
			this.GetClasses = this.GetClasses.bind(this)
			this.ImportMoodleClasses = this.ImportMoodleClasses.bind(this)
			this.ListMoodleCourses = this.ListMoodleCourses.bind(this)
			this.MoodleDbStatus = this.MoodleDbStatus.bind(this)
			this.UpdateClasss = this.UpdateClasss.bind(this)
		}

		public async CreateClass(
			params: ClassBody
		): Promise<BulkClassResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/classes`,
				JSON.stringify(params)
			)
			return (await resp.json()) as BulkClassResponse
		}

		public async DeleteClasss(
			params: DeleteClassRequest
		): Promise<DeleteClassResponse> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				ids: params.ids.map((v) => String(v))
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/classes`,
				undefined,
				{ query }
			)
			return (await resp.json()) as DeleteClassResponse
		}

		public async GetClassById(id: number): Promise<GetClassByIdResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/classes/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as GetClassByIdResponse
		}

		public async GetClasses(
			params: GetClassesRequest
		): Promise<GetClassesResponse> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				ids: params.ids?.map((v) => String(v)),
				unitIds: params.unitIds?.map((v) => String(v))
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/classes`,
				undefined,
				{ query }
			)
			return (await resp.json()) as GetClassesResponse
		}

		public async ImportMoodleClasses(
			params: ImportMoodleClassesBody
		): Promise<ImportMoodleClassesResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/moodle/import-classes`,
				JSON.stringify(params)
			)
			return (await resp.json()) as ImportMoodleClassesResponse
		}

		public async ListMoodleCourses(
			params: ListMoodleCoursesRequest
		): Promise<ListMoodleCoursesResponse> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				unitId:
					params.unitId === undefined
						? undefined
						: String(params.unitId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/moodle/courses`,
				undefined,
				{ query }
			)
			return (await resp.json()) as ListMoodleCoursesResponse
		}

		/**
		 * Kiểm tra kết nối MariaDB/Moodle (không lộ password)
		 */
		public async MoodleDbStatus(): Promise<{
			ok: boolean
			host: string
			port: number
			user: string
			database: string
			version?: string
			courseCount?: number
			error?: string
			syncEnabled: boolean
			passwordSet: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/moodle/status`
			)
			return (await resp.json()) as {
				ok: boolean
				host: string
				port: number
				user: string
				database: string
				version?: string
				courseCount?: number
				error?: string
				syncEnabled: boolean
				passwordSet: boolean
			}
		}

		public async UpdateClasss(params: UpdateClassBody): Promise<void> {
			await this.baseClient.callTypedAPI(
				'PATCH',
				`/classes`,
				JSON.stringify(params)
			)
		}
	}
}

export namespace exam_management {
	export interface AcademicTitleRow {
		id: number
		createdAt: string
		updatedAt: string
		name: string
		percentage: number
		sortOrder: number
	}

	export interface ApprovalBoardItem {
		exam: ExamResponse
		rows: {
			paperNumber: number | null
			questionNumber: number
			content: string
			answer: string
			points: number
		}[]
	}

	export interface AssignmentLogRow {
		id: number
		createdAt: string
		action: string
		subjectId: number | null
		subjectCode: string | null
		subjectName: string | null
		majorCode: string | null
		facultyCode: string | null
		classId: number | null
		classCode: string | null
		className: string | null
		teacherUserId: number | null
		teacherUsername: string | null
		teacherDisplayName: string | null
		note: string | null
		actorUserId: number | null
		actorUsername: string | null
		actorDisplayName: string | null
		summary: string
	}

	export interface AssignmentRow {
		id: number
		createdAt: string
		updatedAt: string
		subjectId: number
		subjectCode: string | null
		subjectName: string | null
		baseCode: string | null
		/**
		 * Hệ → Ngành → Khoa → Môn → Lớp
		 */
		systemId: number | null

		systemCode: string | null
		systemName: string | null
		majorId: number | null
		majorCode: string | null
		majorName: string | null
		facultyId: number | null
		facultyCode: string | null
		facultyName: string | null
		classId: number | null
		classCode: string | null
		className: string | null
		/**
		 * YYYY-MM-DD
		 */
		teachingStart: string | null

		/**
		 * YYYY-MM-DD
		 */
		teachingEnd: string | null

		/**
		 * ACTIVE | EXPIRED | UPCOMING
		 */
		teachingStatus: TeachingPeriodStatus

		teachingStatusLabel: string
		userId: number
		username: string | null
		displayName: string | null
		/**
		 * Khoa trong danh mục GV (exam_teachers) — để UI chỉ hiện đúng khoa
		 */
		teacherFacultyCode: string | null

		teacherFacultyName: string | null
		note: string | null
		assignedByUserId: number | null
		assignedByUsername: string | null
		assignedByDisplayName: string | null
	}

	export interface ClassCatalogResponse {
		id: number
		createdAt: string
		updatedAt: string
		code: string
		name: string
		/**
		 * Hệ đào tạo (từ ngành)
		 */
		systemId?: number | null

		systemCode?: string | null
		systemName?: string | null
		majorId: number | null
		majorCode?: string | null
		majorName?: string | null
		facultyId: number | null
		facultyCode?: string | null
		facultyName?: string | null
		cohort: string | null
		/**
		 * ACTIVE = còn trong khóa (tháng-năm hiện tại ≤ tháng-năm kết thúc)
		 * EXPIRED = hết niên khóa
		 */
		status: ExamClassLifecycleStatus

		statusLabel: string
		/**
		 * Năm kết thúc khóa (từ cohort), null nếu không parse được
		 */
		cohortEndYear: number | null

		/**
		 * YYYY-MM kết thúc khóa
		 */
		cohortEndMonth?: string | null

		description: string | null
	}

	export interface DrawResponse {
		id: number
		createdAt: string
		drawCode: string
		examId: number
		examCode: string | null
		examTitle?: string | null
		/**
		 * Số đề import — trùng với đề được bốc (vd. 9)
		 */
		paperNumber: number | null

		subjectId: number | null
		subjectCode?: string | null
		subjectName?: string | null
		majorId: number | null
		majorCode?: string | null
		majorName?: string | null
		drawType: string
		classId: number | null
		className: string | null
		drawnByUserId: number | null
		drawnByUsername: string | null
		drawnByDisplayName: string | null
		drawnAt: string
		/**
		 * Đã xuất in → vào kho đề đã dùng
		 */
		printedAt: string | null

		printedByUsername: string | null
		/**
		 * |Ngày thi − ngày hiện tại| > 3 → không cho in
		 */
		printBlocked: boolean

		printBlockedAt: string | null
		printBlockedReason: string | null
		/**
		 * Số ngày đã trôi qua từ ngày rút
		 */
		daysSinceDraw: number | null

		examDate: string | null
		examTime: string | null
		location: string | null
		note: string | null
	}

	/**
	 * ACTIVE = còn trong niên khóa; EXPIRED = đã qua tháng/năm kết thúc khóa
	 */
	export type ExamClassLifecycleStatus = 'ACTIVE' | 'EXPIRED'

	export interface ExamDuplicateHit {
		id: number
		code: string
		title: string
		paperNumber: number | null
		status: string
		statusLabel: string
		reason: string
		/**
		 * hard = ≥2 câu hỏi trùng → chặn tạo
		 * soft = 1 câu trùng hoặc cùng số đề / câu khác → cảnh báo, vẫn cho tạo
		 */
		severity: 'hard' | 'soft'
	}

	export interface ExamResponse {
		id: number
		createdAt: string
		updatedAt: string
		code: string
		title: string
		subjectId: number
		subjectCode?: string | null
		subjectName?: string | null
		majorId?: number | null
		majorCode?: string | null
		majorName?: string | null
		status: string
		statusLabel: string
		createdByUserId: number | null
		createdByUsername: string | null
		createdByDisplayName: string | null
		approvedByUserId: number | null
		approvedByUsername: string | null
		approvedByDisplayName: string | null
		approvedAt: string | null
		approvedByRank: string | null
		approvedByPosition: string | null
		approvedBySignatureUrl: string | null
		approvedByTitle: string | null
		/**
		 * CNK duyệt — chữ ký CHỦ NHIỆM KHOA
		 */
		deptHeadUserId: number | null

		deptHeadUsername: string | null
		deptHeadDisplayName: string | null
		deptHeadRank: string | null
		deptHeadSignatureUrl: string | null
		deptHeadApprovedAt: string | null
		qrCode: string | null
		locked: boolean
		/**
		 * Số đề từ import (Đề thi số n)
		 */
		paperNumber: number | null

		/**
		 * Lớp thi gắn khi import
		 */
		classId: number | null

		className: string | null
		durationMinutes: number | null
		questionFileUrl: string | null
		questionFileName: string | null
		answerFileUrl: string | null
		answerFileName: string | null
		note: string | null
		returnNote: string | null
		questions?: QuestionResponse[]
		questionCount?: number
	}

	export interface ExamUsageDraw {
		id: number
		drawCode: string
		drawType: string
		paperNumber: number | null
		classId: number | null
		className: string | null
		drawnAt: string
		printedAt: string | null
		examDate: string | null
		location: string | null
	}

	export interface FacultyHeadRow {
		id: number
		createdAt: string
		updatedAt: string
		facultyCode: string
		facultyName: string | null
		userId: number
		username: string | null
		displayName: string | null
		note: string | null
	}

	export interface FacultyOption {
		code: string
		name: string
	}

	export interface FacultyResponse {
		id: number
		createdAt: string
		updatedAt: string
		code: string
		name: string
		majorId: number | null
		majorCode?: string | null
		majorName?: string | null
		description: string | null
	}

	export interface MajorResponse {
		id: number
		createdAt: string
		updatedAt: string
		code: string
		name: string
		systemId: number
		levelCode: string | null
		shortCode: string | null
		catalogNumber: string | null
		nationalMajorCode: string | null
		qualification: string | null
		trainingDuration: string | null
		trainingForm: string | null
		systemCode?: string | null
		systemName?: string | null
		systemLetter?: string | null
		description: string | null
	}

	export interface QuestionBody {
		questionNumber?: number
		content: string
		answer?: string
		points?: number
	}

	export interface QuestionResponse {
		id: number
		examId: number
		questionNumber: number
		content: string
		answer: string | null
		points: number
	}

	export interface SubjectResponse {
		id: number
		createdAt: string
		updatedAt: string
		code: string
		baseCode: string | null
		name: string
		creditHours: number | null
		lessonHours: number | null
		facultyId: number
		facultyCode?: string | null
		facultyName?: string | null
		majorId: number | null
		majorCode?: string | null
		majorName?: string | null
		/**
		 * Hệ đào tạo (QS/DS) — để UI cố định khi GV import
		 */
		systemId?: number | null

		systemCode?: string | null
		systemName?: string | null
		description: string | null
	}

	export interface SystemResponse {
		id: number
		createdAt: string
		updatedAt: string
		code: string
		name: string
		letter: string
		description: string | null
	}

	export interface TeacherCatalogRow {
		id: number
		createdAt: string
		updatedAt: string
		userId: number
		username: string | null
		displayName: string | null
		facultyCode: string
		facultyName: string | null
		academicTitleId: number | null
		academicTitleName: string | null
		academicTitlePercentage: number | null
		note: string | null
		createdByUserId: number | null
		createdByUsername: string | null
		createdByDisplayName: string | null
	}

	export interface TeacherOption {
		id: number
		username: string
		displayName: string | null
	}

	/**
	 * Trạng thái thời gian giảng dạy trên phân công
	 */
	export type TeachingPeriodStatus = 'ACTIVE' | 'EXPIRED' | 'UPCOMING'

	export interface WorkflowLogResponse {
		id: number
		createdAt: string
		examId: number
		action: string
		fromStatus: string | null
		toStatus: string | null
		note: string | null
		actorUserId: number | null
		actorUsername: string | null
		actorDisplayName: string | null
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.BghApproveBatch = this.BghApproveBatch.bind(this)
			this.CheckExamDuplicates = this.CheckExamDuplicates.bind(this)
			this.CreateExam = this.CreateExam.bind(this)
			this.CreateExamAcademicTitle =
				this.CreateExamAcademicTitle.bind(this)
			this.CreateExamAssignment = this.CreateExamAssignment.bind(this)
			this.CreateExamClass = this.CreateExamClass.bind(this)
			this.CreateExamFaculty = this.CreateExamFaculty.bind(this)
			this.CreateExamMajor = this.CreateExamMajor.bind(this)
			this.CreateExamSubject = this.CreateExamSubject.bind(this)
			this.CreateExamSystem = this.CreateExamSystem.bind(this)
			this.CreateExamTeacherCatalog =
				this.CreateExamTeacherCatalog.bind(this)
			this.DecideExam = this.DecideExam.bind(this)
			this.DeleteExam = this.DeleteExam.bind(this)
			this.DeleteExamAcademicTitle =
				this.DeleteExamAcademicTitle.bind(this)
			this.DeleteExamAssignment = this.DeleteExamAssignment.bind(this)
			this.DeleteExamClass = this.DeleteExamClass.bind(this)
			this.DeleteExamFaculty = this.DeleteExamFaculty.bind(this)
			this.DeleteExamFacultyHead = this.DeleteExamFacultyHead.bind(this)
			this.DeleteExamMajor = this.DeleteExamMajor.bind(this)
			this.DeleteExamSubject = this.DeleteExamSubject.bind(this)
			this.DeleteExamSystem = this.DeleteExamSystem.bind(this)
			this.DeleteExamTeacherCatalog =
				this.DeleteExamTeacherCatalog.bind(this)
			this.DrawExam = this.DrawExam.bind(this)
			this.ExportDrawMinutes = this.ExportDrawMinutes.bind(this)
			this.ExportDrawPrint = this.ExportDrawPrint.bind(this)
			this.ExportExamPackage = this.ExportExamPackage.bind(this)
			this.ExportExamPackageBundle =
				this.ExportExamPackageBundle.bind(this)
			this.ExportExamPackageSelected =
				this.ExportExamPackageSelected.bind(this)
			this.ExportTeacherExamBoard = this.ExportTeacherExamBoard.bind(this)
			this.GenerateExamQr = this.GenerateExamQr.bind(this)
			this.GetExam = this.GetExam.bind(this)
			this.GetExamDraw = this.GetExamDraw.bind(this)
			this.GetExamDrawPool = this.GetExamDrawPool.bind(this)
			this.GetPendingExamCount = this.GetPendingExamCount.bind(this)
			this.ListApprovalBoard = this.ListApprovalBoard.bind(this)
			this.ListBghBoard = this.ListBghBoard.bind(this)
			this.ListExamAcademicTitles = this.ListExamAcademicTitles.bind(this)
			this.ListExamAssignmentLogs = this.ListExamAssignmentLogs.bind(this)
			this.ListExamAssignments = this.ListExamAssignments.bind(this)
			this.ListExamClasses = this.ListExamClasses.bind(this)
			this.ListExamDraws = this.ListExamDraws.bind(this)
			this.ListExamFaculties = this.ListExamFaculties.bind(this)
			this.ListExamFacultyHeads = this.ListExamFacultyHeads.bind(this)
			this.ListExamFacultyOptions = this.ListExamFacultyOptions.bind(this)
			this.ListExamMajors = this.ListExamMajors.bind(this)
			this.ListExamSubjects = this.ListExamSubjects.bind(this)
			this.ListExamSystems = this.ListExamSystems.bind(this)
			this.ListExamTeacherCandidates =
				this.ListExamTeacherCandidates.bind(this)
			this.ListExamTeacherCatalog = this.ListExamTeacherCatalog.bind(this)
			this.ListExamTeachers = this.ListExamTeachers.bind(this)
			this.ListExamWorkflowLogs = this.ListExamWorkflowLogs.bind(this)
			this.ListExams = this.ListExams.bind(this)
			this.ListOverdueDraws = this.ListOverdueDraws.bind(this)
			this.ListUsedExamVault = this.ListUsedExamVault.bind(this)
			this.LookupDrawByCode = this.LookupDrawByCode.bind(this)
			this.LookupExamByQr = this.LookupExamByQr.bind(this)
			this.SubmitExam = this.SubmitExam.bind(this)
			this.UpdateExam = this.UpdateExam.bind(this)
			this.UpdateExamAcademicTitle =
				this.UpdateExamAcademicTitle.bind(this)
			this.UpdateExamAssignment = this.UpdateExamAssignment.bind(this)
			this.UpdateExamClass = this.UpdateExamClass.bind(this)
			this.UpdateExamFaculty = this.UpdateExamFaculty.bind(this)
			this.UpdateExamMajor = this.UpdateExamMajor.bind(this)
			this.UpdateExamSubject = this.UpdateExamSubject.bind(this)
			this.UpdateExamSystem = this.UpdateExamSystem.bind(this)
			this.UpdateExamTeacherCatalog =
				this.UpdateExamTeacherCatalog.bind(this)
			this.UpsertExamFacultyHead = this.UpsertExamFacultyHead.bind(this)
		}

		/**
		 * BGH phê duyệt hàng loạt theo số đề (paperNumber) hoặc id.
		 * numbersText: "1,3,9" hoặc "1 3 9"
		 */
		public async BghApproveBatch(params: {
			examIds?: number[]
			paperNumbers?: number[]
			/**
			 * vd. "1, 3, 5-7" hoặc "1 9 10"
			 */
			numbersText?: string

			/**
			 * Phạm vi duyệt theo môn
			 */
			subjectId?: number

			/**
			 * Phạm vi duyệt theo ngành đào tạo
			 */
			majorId?: number

			note?: string
		}): Promise<{
			approved: number
			failed: {
				id?: number
				paperNumber?: number
				error: string
			}[]
			data: ExamResponse[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/bgh-approve-batch`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				approved: number
				failed: {
					id?: number
					paperNumber?: number
					error: string
				}[]
				data: ExamResponse[]
			}
		}

		/**
		 * Dò trùng **từng đề** trong cùng môn (kể cả đề đã vào ngân hàng / nháp / đang duyệt).
		 * - Chặn (hard): **≥2 câu hỏi** trùng nội dung với một đề đã có, hoặc với đề khác trong cùng file.
		 * - Cảnh báo (soft): chỉ 1 câu trùng, hoặc cùng số đề nhưng không câu nào giống → vẫn cho import.
		 */
		public async CheckExamDuplicates(params: {
			subjectId: number
			papers: {
				paperNumber?: number | null
				title?: string
				questions?: {
					content?: string
					answer?: string
				}[]
			}[]
		}): Promise<{
			data: {
				index: number
				paperNumber: number | null
				title: string | null
				/**
				 * Chỉ hard — client bỏ qua / chặn
				 */
				duplicates: ExamDuplicateHit[]

				/**
				 * Soft — 1 câu trùng hoặc cùng số đề
				 */
				warnings: ExamDuplicateHit[]

				/**
				 * true nếu có hard duplicate (≥2 câu)
				 */
				blocked: boolean
			}[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/check-duplicates`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: {
					index: number
					paperNumber: number | null
					title: string | null
					/**
					 * Chỉ hard — client bỏ qua / chặn
					 */
					duplicates: ExamDuplicateHit[]

					/**
					 * Soft — 1 câu trùng hoặc cùng số đề
					 */
					warnings: ExamDuplicateHit[]

					/**
					 * true nếu có hard duplicate (≥2 câu)
					 */
					blocked: boolean
				}[]
			}
		}

		public async CreateExam(params: {
			title: string
			subjectId: number
			note?: string
			/**
			 * Số đề từ file import (Đề thi số n / cột Đề số)
			 */
			paperNumber?: number | null

			/**
			 * Bắt buộc: lớp thi khi import (GV dạy môn → chọn lớp)
			 */
			classId?: number | null

			className?: string | null
			durationMinutes?: number | null
			questionFileUrl?: string
			questionFileName?: string
			answerFileUrl?: string
			answerFileName?: string
			questions?: QuestionBody[]
			/**
			 * true = bỏ qua chặn trùng (không khuyến nghị)
			 */
			allowDuplicate?: boolean
		}): Promise<{
			data: ExamResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/exams`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ExamResponse
			}
		}

		public async CreateExamAcademicTitle(params: {
			name: string
			percentage: number
			sortOrder?: number
		}): Promise<{
			data: AcademicTitleRow
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/academic-titles`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: AcademicTitleRow
			}
		}

		public async CreateExamAssignment(params: {
			subjectId: number
			userId: number
			/**
			 * Bắt buộc — phân công theo lớp
			 */
			classId: number

			/**
			 * YYYY-MM-DD — bắt đầu giảng dạy
			 */
			teachingStart?: string | null

			/**
			 * YYYY-MM-DD — kết thúc; hết hạn → không import đề lớp này
			 */
			teachingEnd?: string | null

			note?: string
		}): Promise<{
			data: AssignmentRow
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/assignments`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: AssignmentRow
			}
		}

		public async CreateExamClass(params: {
			code: string
			name: string
			/**
			 * Bắt buộc — lớp thuộc ngành (và hệ qua ngành)
			 */
			majorId: number

			facultyId?: number | null
			cohort?: string
			description?: string
		}): Promise<{
			data: ClassCatalogResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/classes`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ClassCatalogResponse
			}
		}

		public async CreateExamFaculty(params: {
			code: string
			name: string
			majorId?: number | null
			description?: string
		}): Promise<{
			data: FacultyResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/faculties`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: FacultyResponse
			}
		}

		public async CreateExamMajor(params: {
			name: string
			systemId: number
			levelCode?: string | null
			shortCode?: string | null
			code?: string | null
			catalogNumber?: string | null
			nationalMajorCode?: string | null
			qualification?: string | null
			trainingDuration?: string | null
			trainingForm?: string | null
			description?: string
		}): Promise<{
			data: MajorResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/majors`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: MajorResponse
			}
		}

		public async CreateExamSubject(params: {
			name: string
			facultyId: number
			majorId?: number
			sourceSubjectId?: number
			baseCode?: string
			code?: string
			creditHours?: number
			lessonHours?: number
			description?: string
		}): Promise<{
			data: SubjectResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/subjects`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: SubjectResponse
			}
		}

		public async CreateExamSystem(params: {
			code: string
			name: string
			letter: string
			trainingTypeId?: number
			description?: string
		}): Promise<{
			data: SystemResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/systems`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: SystemResponse
			}
		}

		/**
		 * Thêm GV vào danh mục khoa — mỗi người 1 tài khoản riêng (user_id unique).
		 *
		 * Cách 1: gắn tài khoản có sẵn (`userId`)
		 * Cách 2: tạo tài khoản mới (`username` + `password` + `displayName`) rồi gắn khoa
		 */
		public async CreateExamTeacherCatalog(params: {
			/**
			 * Gắn user đã có
			 */
			userId?: number

			/**
			 * Tạo tài khoản mới (mỗi GV 1 TK)
			 */
			username?: string

			password?: string
			displayName?: string
			facultyCode: string
			academicTitleId: number
			note?: string
		}): Promise<{
			data: TeacherCatalogRow
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/teacher-catalog`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: TeacherCatalogRow
			}
		}

		/**
		 * Duyệt / trả lại — đúng 3 cấp tuần tự:
		 * CNK (PENDING_DEPT) → Ban KT (PENDING_EXAM_OFFICE) → BGH (PENDING_BGH) → APPROVED
		 * decision: APPROVE | RETURN
		 */
		public async DecideExam(
			id: number,
			params: {
				decision: 'APPROVE' | 'RETURN'
				note?: string
			}
		): Promise<{
			data: ExamResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/exams/${encodeURIComponent(id)}/decide`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ExamResponse
			}
		}

		public async DeleteExam(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/exam/exams/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteExamAcademicTitle(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/exam/academic-titles/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteExamAssignment(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/exam/assignments/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteExamClass(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/exam/classes/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteExamFaculty(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/exam/faculties/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteExamFacultyHead(facultyCode: string): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/exam/faculty-heads/${encodeURIComponent(facultyCode)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteExamMajor(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/exam/majors/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteExamSubject(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/exam/subjects/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteExamSystem(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/exam/systems/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		/**
		 * Gỡ GV khỏi danh mục khoa (không xóa user)
		 */
		public async DeleteExamTeacherCatalog(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/exam/teacher-catalog/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		/**
		 * Rút ngẫu nhiên 1 đề APPROVED chưa từng bốc.
		 *
		 * Nghiệp vụ lớp thi:
		 * - Mỗi lớp cần **2 phiếu**: 1 Chẵn + 1 Lẻ (2 đề khác nhau trong ngân hàng).
		 * - Rút Chẵn xong vẫn rút được Lẻ (và ngược lại), miễn còn đề trong pool.
		 * - Không rút trùng cùng loại (Chẵn/Lẻ) cho cùng lớp + cùng môn.
		 * - Không tái sử dụng cùng 1 mã đề đã bốc (cho bất kỳ lớp nào).
		 */
		public async DrawExam(params: {
			subjectId: number
			drawType: 'EVEN' | 'ODD'
			classId?: number
			className?: string
			examDate?: string
			examTime?: string
			location?: string
			note?: string
		}): Promise<{
			data: DrawResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/draws`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: DrawResponse
			}
		}

		/**
		 * Biên bản bốc thăm đề thi (form trước/sau khi rút).
		 * Có thể gọi theo subjectId + classId (chưa rút) hoặc draw ids (đã rút).
		 */
		public async ExportDrawMinutes(params: {
			subjectId: number
			classId?: number
			/**
			 * optional: ids phiếu chẵn/lẻ đã rút, cách nhau bằng dấu phẩy
			 */
			drawIds?: string

			location?: string
			proctorName?: string
			studentName?: string
			studentClass?: string
		}): Promise<{
			filename: string
			contentType: string
			html: string
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				classId:
					params.classId === undefined
						? undefined
						: String(params.classId),
				drawIds: params.drawIds,
				location: params.location,
				proctorName: params.proctorName,
				studentClass: params.studentClass,
				studentName: params.studentName,
				subjectId: String(params.subjectId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/draw-minutes`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				filename: string
				contentType: string
				html: string
			}
		}

		/**
		 * Xuất đề / đáp án để in — form chuẩn «ĐỀ THI HẾT HỌC PHẦN».
		 * kind: questions | answers
		 * Chặn in nếu ngày thi và ngày hiện tại chênh quá 3 ngày.
		 */
		public async ExportDrawPrint(
			id: number,
			params: {
				kind?: string
			}
		): Promise<{
			filename: string
			contentType: string
			html: string
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				kind: params.kind
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/draws/${encodeURIComponent(id)}/export`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				filename: string
				contentType: string
				html: string
			}
		}

		/**
		 * Xuất bộ đề 1 mã đề (I + II + chữ ký BGH + CNK) — form giấy.
		 * withSignatures=false → để trống ô ký (ký tay).
		 */
		public async ExportExamPackage(
			id: number,
			params: {
				withSignatures?: boolean
			}
		): Promise<{
			filename: string
			contentType: string
			html: string
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				withSignatures:
					params.withSignatures === undefined
						? undefined
						: String(params.withSignatures)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/exams/${encodeURIComponent(id)}/export-package`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				filename: string
				contentType: string
				html: string
			}
		}

		/**
		 * Xuất gộp nhiều đề cùng môn + lớp (nhiều «Đề số n» như mẫu import).
		 * Chỉ gồm đề đã BGH phê duyệt (APPROVED).
		 */
		public async ExportExamPackageBundle(params: {
			subjectId: number
			classId?: number
			/**
			 * true = kèm chữ ký số; false = để trống ô ký
			 */
			withSignatures?: boolean
		}): Promise<{
			filename: string
			contentType: string
			html: string
			paperCount: number
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				classId:
					params.classId === undefined
						? undefined
						: String(params.classId),
				subjectId: String(params.subjectId),
				withSignatures:
					params.withSignatures === undefined
						? undefined
						: String(params.withSignatures)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/export-package-bundle`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				filename: string
				contentType: string
				html: string
				paperCount: number
			}
		}

		/**
		 * In / xuất bộ đề theo danh sách id đã chọn (ngân hàng hoặc hàng đợi duyệt).
		 * Form giấy mẫu C. CNK/KT/BGH: đề đã gửi duyệt hoặc APPROVED.
		 * withSignatures=false → để trống ô ký.
		 */
		public async ExportExamPackageSelected(params: {
			/**
			 * CSV id, vd 1,2,5
			 */
			examIds: string

			withSignatures?: boolean
			/**
			 * true = cho phép đề đang duyệt (PENDING_*) — dùng trang duyệt đề.
			 * false/default = chỉ APPROVED (ngân hàng).
			 */
			forReview?: boolean
		}): Promise<{
			filename: string
			contentType: string
			html: string
			paperCount: number
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				examIds: params.examIds,
				forReview:
					params.forReview === undefined
						? undefined
						: String(params.forReview),
				withSignatures:
					params.withSignatures === undefined
						? undefined
						: String(params.withSignatures)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/export-package-selected`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				filename: string
				contentType: string
				html: string
				paperCount: number
			}
		}

		/**
		 * Xuất bảng đề thi cho GV — 3 mẫu:
		 * A = ĐỀ THI SỐ n + Đáp án - Thang điểm
		 * B = bảng gộp
		 * C = form giấy BỘ CÂU HỎI - ĐÁP ÁN (biểu mẫu chính thức)
		 *
		 * **Chỉ đề đã BGH phê duyệt (APPROVED).**
		 * **GV:** chỉ xuất đề của mình và **chỉ sau khi hết thời gian giảng dạy** lớp đó.
		 * withSignatures=false → form C để trống ô ký.
		 */
		public async ExportTeacherExamBoard(params: {
			/**
			 * a | b | c — mặc định c (form giấy)
			 */
			format: string

			subjectId?: number
			/**
			 * CSV id đề, vd 1,2,3 — vẫn chỉ lấy những id đã APPROVED
			 */
			examIds?: string

			withSignatures?: boolean
			/**
			 * true = CNK/KT/BGH xuất đề đang duyệt (PENDING_*) — mẫu A/B, không bắt APPROVED.
			 */
			forReview?: boolean
		}): Promise<{
			filename: string
			contentType: string
			html: string
			format: string
			paperCount: number
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				examIds: params.examIds,
				forReview:
					params.forReview === undefined
						? undefined
						: String(params.forReview),
				format: params.format,
				subjectId:
					params.subjectId === undefined
						? undefined
						: String(params.subjectId),
				withSignatures:
					params.withSignatures === undefined
						? undefined
						: String(params.withSignatures)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/export-teacher-board`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				filename: string
				contentType: string
				html: string
				format: string
				paperCount: number
			}
		}

		/**
		 * BGH tạo / tái tạo QR (nếu cần)
		 */
		public async GenerateExamQr(id: number): Promise<{
			data: ExamResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/exams/${encodeURIComponent(id)}/generate-qr`
			)
			return (await resp.json()) as {
				data: ExamResponse
			}
		}

		public async GetExam(id: number): Promise<{
			data: ExamResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/exams/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: ExamResponse
			}
		}

		public async GetExamDraw(id: number): Promise<{
			data: DrawResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/draws/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: DrawResponse
			}
		}

		/**
		 * Pool rút đề: số đề APPROVED còn lại (chưa bốc) theo môn.
		 * Mỗi lớp cần 2 phiếu (Chẵn + Lẻ) = 2 đề khác nhau trong pool.
		 */
		public async GetExamDrawPool(params: {
			subjectId: number
			classId?: number
		}): Promise<{
			subjectId: number
			approvedTotal: number
			usedCount: number
			availableCount: number
			/**
			 * Với lớp đã chọn: đã có phiếu Chẵn / Lẻ chưa
			 */
			classHasEven: boolean

			classHasOdd: boolean
			className: string | null
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				classId:
					params.classId === undefined
						? undefined
						: String(params.classId),
				subjectId: String(params.subjectId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/draw-pool`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				subjectId: number
				approvedTotal: number
				usedCount: number
				availableCount: number
				/**
				 * Với lớp đã chọn: đã có phiếu Chẵn / Lẻ chưa
				 */
				classHasEven: boolean

				classHasOdd: boolean
				className: string | null
			}
		}

		public async GetPendingExamCount(): Promise<{
			count: number
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/pending-count`
			)
			return (await resp.json()) as {
				count: number
			}
		}

		/**
		 * Bảng duyệt theo cấp (CNK / Ban KT / BGH):
		 * Chỉ đề GV đã gửi (pending đúng cấp của người xem).
		 * Nội dung: Đề số | Câu | Nội dung | Đáp án | Điểm — theo ngành / môn.
		 * BGH phê duyệt cuối → APPROVED → ngân hàng đề.
		 */
		public async ListApprovalBoard(params: {
			majorId?: number
			subjectId?: number
		}): Promise<{
			/**
			 * Cấp duyệt của user hiện tại
			 */
			level: string

			levelLabel: string
			data: ApprovalBoardItem[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				majorId:
					params.majorId === undefined
						? undefined
						: String(params.majorId),
				subjectId:
					params.subjectId === undefined
						? undefined
						: String(params.subjectId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/approval-board`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				/**
				 * Cấp duyệt của user hiện tại
				 */
				level: string

				levelLabel: string
				data: ApprovalBoardItem[]
			}
		}

		/**
		 * Alias BGH (giữ path cũ)
		 */
		public async ListBghBoard(params: {
			majorId?: number
			subjectId?: number
		}): Promise<{
			data: ApprovalBoardItem[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				majorId:
					params.majorId === undefined
						? undefined
						: String(params.majorId),
				subjectId:
					params.subjectId === undefined
						? undefined
						: String(params.subjectId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/bgh-board`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: ApprovalBoardItem[]
			}
		}

		/**
		 * Danh mục chức danh dùng khi khai báo giáo viên.
		 */
		public async ListExamAcademicTitles(): Promise<{
			data: AcademicTitleRow[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/academic-titles`
			)
			return (await resp.json()) as {
				data: AcademicTitleRow[]
			}
		}

		public async ListExamAssignmentLogs(params: {
			limit?: number
			subjectId?: number
			userId?: number
		}): Promise<{
			data: AssignmentLogRow[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				limit:
					params.limit === undefined
						? undefined
						: String(params.limit),
				subjectId:
					params.subjectId === undefined
						? undefined
						: String(params.subjectId),
				userId:
					params.userId === undefined
						? undefined
						: String(params.userId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/assignment-logs`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: AssignmentLogRow[]
			}
		}

		public async ListExamAssignments(params: {
			subjectId?: number
			userId?: number
			majorId?: number
			facultyId?: number
			classId?: number
			/**
			 * GV: chỉ phân công của mình (soạn đề theo hệ/ngành/khoa/lớp)
			 */
			mine?: boolean

			q?: string
		}): Promise<{
			data: AssignmentRow[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				classId:
					params.classId === undefined
						? undefined
						: String(params.classId),
				facultyId:
					params.facultyId === undefined
						? undefined
						: String(params.facultyId),
				majorId:
					params.majorId === undefined
						? undefined
						: String(params.majorId),
				mine:
					params.mine === undefined ? undefined : String(params.mine),
				q: params.q,
				subjectId:
					params.subjectId === undefined
						? undefined
						: String(params.subjectId),
				userId:
					params.userId === undefined
						? undefined
						: String(params.userId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/assignments`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: AssignmentRow[]
			}
		}

		public async ListExamClasses(params: {
			q?: string
			/**
			 * Lọc theo hệ đào tạo (QS/DS)
			 */
			systemId?: number

			majorId?: number
			facultyId?: number
		}): Promise<{
			data: ClassCatalogResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				facultyId:
					params.facultyId === undefined
						? undefined
						: String(params.facultyId),
				majorId:
					params.majorId === undefined
						? undefined
						: String(params.majorId),
				q: params.q,
				systemId:
					params.systemId === undefined
						? undefined
						: String(params.systemId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/classes`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: ClassCatalogResponse[]
			}
		}

		public async ListExamDraws(params: {
			subjectId?: number
			drawCode?: string
			/**
			 * Mã đề (DT-…)
			 */
			examCode?: string

			classId?: number
			/**
			 * Tên lớp (like)
			 */
			className?: string

			/**
			 * Ngày thi (YYYY-MM-DD)
			 */
			examDate?: string

			/**
			 * Ngày rút đề (YYYY-MM-DD) — khớp prefix drawn_at
			 */
			drawnDate?: string

			drawType?: string
			q?: string
		}): Promise<{
			data: DrawResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				classId:
					params.classId === undefined
						? undefined
						: String(params.classId),
				className: params.className,
				drawCode: params.drawCode,
				drawType: params.drawType,
				drawnDate: params.drawnDate,
				examCode: params.examCode,
				examDate: params.examDate,
				q: params.q,
				subjectId:
					params.subjectId === undefined
						? undefined
						: String(params.subjectId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/draws`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: DrawResponse[]
			}
		}

		public async ListExamFaculties(params: {
			q?: string
			majorId?: number
		}): Promise<{
			data: FacultyResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				majorId:
					params.majorId === undefined
						? undefined
						: String(params.majorId),
				q: params.q
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/faculties`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: FacultyResponse[]
			}
		}

		/**
		 * Danh sách Chủ nhiệm khoa hiện tại để hiển thị trong danh mục khoa.
		 */
		public async ListExamFacultyHeads(): Promise<{
			data: FacultyHeadRow[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/faculty-heads`
			)
			return (await resp.json()) as {
				data: FacultyHeadRow[]
			}
		}

		/**
		 * Danh sách mã khoa (unique K1…K8) từ DMĐT
		 */
		public async ListExamFacultyOptions(): Promise<{
			data: FacultyOption[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/faculty-options`
			)
			return (await resp.json()) as {
				data: FacultyOption[]
			}
		}

		public async ListExamMajors(params: {
			q?: string
			systemId?: number
		}): Promise<{
			data: MajorResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				q: params.q,
				systemId:
					params.systemId === undefined
						? undefined
						: String(params.systemId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/majors`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: MajorResponse[]
			}
		}

		public async ListExamSubjects(params: {
			majorId?: number
			facultyId?: number
			q?: string
			mine?: boolean
		}): Promise<{
			data: SubjectResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				facultyId:
					params.facultyId === undefined
						? undefined
						: String(params.facultyId),
				majorId:
					params.majorId === undefined
						? undefined
						: String(params.majorId),
				mine:
					params.mine === undefined ? undefined : String(params.mine),
				q: params.q
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/subjects`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: SubjectResponse[]
			}
		}

		public async ListExamSystems(params: { q?: string }): Promise<{
			data: SystemResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				q: params.q
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/systems`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: SystemResponse[]
			}
		}

		/**
		 * User có role exam_lecturer chưa vào danh mục — để CNK thêm
		 */
		public async ListExamTeacherCandidates(params: {
			q?: string
		}): Promise<{
			data: TeacherOption[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				q: params.q
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/teacher-candidates`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: TeacherOption[]
			}
		}

		/**
		 * Danh mục giáo viên theo khoa — mỗi user 1 dòng (không trùng).
		 * Lọc theo khoa / từ khóa. CNK chỉ thấy GV thuộc khoa trong ngành mình.
		 */
		public async ListExamTeacherCatalog(params: {
			facultyCode?: string
			q?: string
		}): Promise<{
			data: TeacherCatalogRow[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				facultyCode: params.facultyCode,
				q: params.q
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/teacher-catalog`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: TeacherCatalogRow[]
			}
		}

		/**
		 * Danh sách GV để chọn khi phân công — lấy từ danh mục (unique).
		 * Fallback: role exam_lecturer nếu danh mục trống (admin).
		 */
		public async ListExamTeachers(params: {
			q?: string
			facultyCode?: string
		}): Promise<{
			data: {
				id: number
				username: string
				displayName: string | null
				facultyCode?: string | null
				facultyName?: string | null
			}[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				facultyCode: params.facultyCode,
				q: params.q
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/teachers`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: {
					id: number
					username: string
					displayName: string | null
					facultyCode?: string | null
					facultyName?: string | null
				}[]
			}
		}

		public async ListExamWorkflowLogs(id: number): Promise<{
			data: WorkflowLogResponse[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/exams/${encodeURIComponent(id)}/logs`
			)
			return (await resp.json()) as {
				data: WorkflowLogResponse[]
			}
		}

		public async ListExams(params: {
			status?: string
			subjectId?: number
			majorId?: number
			mine?: boolean
			bank?: boolean
			pending?: boolean
		}): Promise<{
			data: ExamResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				bank:
					params.bank === undefined ? undefined : String(params.bank),
				majorId:
					params.majorId === undefined
						? undefined
						: String(params.majorId),
				mine:
					params.mine === undefined ? undefined : String(params.mine),
				pending:
					params.pending === undefined
						? undefined
						: String(params.pending),
				status: params.status,
				subjectId:
					params.subjectId === undefined
						? undefined
						: String(params.subjectId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/exams`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: ExamResponse[]
			}
		}

		/**
		 * Danh sách phiếu có ngày thi và ngày hiện tại chênh quá 3 ngày.
		 * Đồng thời lazy-mark print_blocked.
		 */
		public async ListOverdueDraws(): Promise<{
			data: DrawResponse[]
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/draws-overdue`
			)
			return (await resp.json()) as {
				data: DrawResponse[]
			}
		}

		/**
		 * Kho đề đã sử dụng / đã in (phiếu bốc).
		 * printedOnly=true → chỉ đề đã xuất in.
		 */
		public async ListUsedExamVault(params: {
			subjectId?: number
			printedOnly?: boolean
			q?: string
		}): Promise<{
			data: DrawResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				printedOnly:
					params.printedOnly === undefined
						? undefined
						: String(params.printedOnly),
				q: params.q,
				subjectId:
					params.subjectId === undefined
						? undefined
						: String(params.subjectId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/used-vault`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: DrawResponse[]
			}
		}

		/**
		 * Tra cứu theo mã bốc đề
		 */
		public async LookupDrawByCode(drawCode: string): Promise<{
			data: DrawResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/exam/draw-by-code/${encodeURIComponent(drawCode)}`
			)
			return (await resp.json()) as {
				data: DrawResponse
			}
		}

		/**
		 * Tra cứu đề theo nội dung mã QR (quét / dán text / payload từ ảnh).
		 * Trả về đề + các phiếu bốc đang / đã sử dụng (in).
		 */
		public async LookupExamByQr(params: { qrText: string }): Promise<{
			data: {
				exam: ExamResponse
				/**
				 * Phiếu bốc liên quan (đang dùng / đã in)
				 */
				usage: ExamUsageDraw[]

				/**
				 * Phiếu mới nhất (đang sử dụng nếu có)
				 */
				activeUse: ExamUsageDraw | null

				matchedBy: string
				parsed: {
					examId: number | null
					examCode: string | null
					paperNumber: number | null
				}
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/lookup-qr`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: {
					exam: ExamResponse
					/**
					 * Phiếu bốc liên quan (đang dùng / đã in)
					 */
					usage: ExamUsageDraw[]

					/**
					 * Phiếu mới nhất (đang sử dụng nếu có)
					 */
					activeUse: ExamUsageDraw | null

					matchedBy: string
					parsed: {
						examId: number | null
						examCode: string | null
						paperNumber: number | null
					}
				}
			}
		}

		/**
		 * GV gửi đề → CNK
		 */
		public async SubmitExam(
			id: number,
			params: {
				note?: string
			}
		): Promise<{
			data: ExamResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/exams/${encodeURIComponent(id)}/submit`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ExamResponse
			}
		}

		public async UpdateExam(
			id: number,
			params: {
				title?: string
				note?: string
				classId?: number | null
				className?: string | null
				durationMinutes?: number | null
				questionFileUrl?: string | null
				questionFileName?: string | null
				answerFileUrl?: string | null
				answerFileName?: string | null
				questions?: QuestionBody[]
			}
		): Promise<{
			data: ExamResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PUT',
				`/exam/exams/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ExamResponse
			}
		}

		public async UpdateExamAcademicTitle(
			id: number,
			params: {
				name: string
				percentage: number
				sortOrder?: number
			}
		): Promise<{
			data: AcademicTitleRow
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/exam/academic-titles/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: AcademicTitleRow
			}
		}

		public async UpdateExamAssignment(
			id: number,
			params: {
				subjectId?: number
				userId?: number
				classId?: number | null
				teachingStart?: string | null
				teachingEnd?: string | null
				note?: string | null
			}
		): Promise<{
			data: AssignmentRow
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PUT',
				`/exam/assignments/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: AssignmentRow
			}
		}

		public async UpdateExamClass(
			id: number,
			params: {
				code?: string
				name?: string
				majorId?: number | null
				facultyId?: number | null
				cohort?: string | null
				description?: string | null
			}
		): Promise<{
			data: ClassCatalogResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PUT',
				`/exam/classes/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ClassCatalogResponse
			}
		}

		public async UpdateExamFaculty(
			id: number,
			params: {
				code?: string
				name?: string
				majorId?: number
				description?: string | null
			}
		): Promise<{
			data: FacultyResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PUT',
				`/exam/faculties/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: FacultyResponse
			}
		}

		public async UpdateExamMajor(
			id: number,
			params: {
				code?: string
				name?: string
				levelCode?: string | null
				shortCode?: string | null
				catalogNumber?: string | null
				nationalMajorCode?: string | null
				qualification?: string | null
				trainingDuration?: string | null
				trainingForm?: string | null
				systemId?: number
				description?: string | null
			}
		): Promise<{
			data: MajorResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PUT',
				`/exam/majors/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: MajorResponse
			}
		}

		public async UpdateExamSubject(
			id: number,
			params: {
				name?: string
				facultyId?: number
				baseCode?: string
				code?: string
				creditHours?: number
				lessonHours?: number
				description?: string | null
			}
		): Promise<{
			data: SubjectResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PUT',
				`/exam/subjects/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: SubjectResponse
			}
		}

		public async UpdateExamSystem(
			id: number,
			params: {
				code?: string
				name?: string
				letter?: string
				trainingTypeId?: number
				description?: string | null
			}
		): Promise<{
			data: SystemResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PUT',
				`/exam/systems/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: SystemResponse
			}
		}

		/**
		 * Cập nhật khoa / tên hiển thị GV trong danh mục
		 */
		public async UpdateExamTeacherCatalog(
			id: number,
			params: {
				facultyCode?: string
				academicTitleId?: number
				displayName?: string
				note?: string | null
			}
		): Promise<{
			data: TeacherCatalogRow
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/exam/teacher-catalog/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: TeacherCatalogRow
			}
		}

		/**
		 * Gán / cập nhật Chủ nhiệm khoa theo mã khoa (K1…K8).
		 * 1 khoa = 1 CNK chính. Chức vụ «Chủ nhiệm khoa» gắn tài khoản — không đơn vị.
		 * Chỉ super admin (tạo TK từ danh sách user).
		 */
		public async UpsertExamFacultyHead(params: {
			userId: number
			facultyCode: string
			note?: string
		}): Promise<{
			data: FacultyHeadRow
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/exam/faculty-heads`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: FacultyHeadRow
			}
		}
	}
}

export namespace healthcheck {
	export interface HeathCheckResponse {
		uptime: string
		timestamp: number
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.Heathz = this.Heathz.bind(this)
		}

		public async Heathz(): Promise<HeathCheckResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI('POST', `/healthz`)
			return (await resp.json()) as HeathCheckResponse
		}
	}
}

export namespace leave_management {
	export interface ConvertWordTemplateRequest {
		fileName: string
		base64: string
	}

	export interface ConvertWordTemplateResponse {
		fileName: string
		base64: string
	}

	export interface CreateLeaveBody {
		leaveType?: string
		requestScope?: 'INDIVIDUAL' | 'CLASS' | 'SHORT_LEAVE'
		classId?: number | null
		className?: string | null
		/**
		 * Chỉ huy/đại đội nhập trực tiếp, không tính theo thâm niên
		 */
		manualDays?: number

		personnelId?: number | null
		/**
		 * Đối tượng cố định từ profile — server ưu tiên profile
		 */
		objectType?: string

		rank?: string | null
		unitId?: number | null
		unitName?: string | null
		travelDays?: number
		extraDays?: number
		/**
		 * Lý do nghỉ thêm (ANNUAL) hoặc lý do phép đặc biệt (SPECIAL)
		 */
		extraReasons?: string[]

		/**
		 * Số ngày phép đặc biệt (1–10), chỉ dùng khi leaveType=SPECIAL
		 */
		specialDays?: number

		startDate?: string | null
		endDate?: string | null
		localityId?: number | null
		/**
		 * Địa chỉ cụ thể (số nhà, đường…) — ghép vào localityPath
		 */
		localityDetail?: string | null

		note?: string | null
		replacementPersonnelId?: number | null
	}

	export interface CreateLocalityBody {
		name: string
		level: string
		parentId?: number | null
		code?: string | null
	}

	export interface CreatePersonnelBody {
		/**
		 * Bỏ trống → hệ thống tự sinh (QN-xxxxxx)
		 */
		code?: string | null

		fullName: string
		enlistmentDate?: string | null
		recruitment?: string | null
		objectType: string
		rank?: string | null
		position?: string | null
		classId?: number | null
		unitId?: number | null
		unitName?: string | null
		hometown?: string | null
		permanentResidence?: string | null
		userId?: number | null
		email?: string | null
		commanderUserId?: number | null
		commanderName?: string | null
		className?: string | null
		managementArea?: string
	}

	export interface CreateRegulationBody {
		leaveType: string
		objectType?: string | null
		minYears?: number | null
		maxYears?: number | null
		baseDays: number
		label?: string | null
		description?: string | null
		isActive?: boolean
	}

	export interface ImportLocalityItem {
		/**
		 * Tên tỉnh (bắt buộc)
		 */
		province: string

		/**
		 * Tên xã/phường (tuỳ chọn)
		 */
		ward?: string | null

		/**
		 * Tên thôn (tuỳ chọn — cần ward)
		 */
		village?: string | null

		provinceCode?: string | null
		wardCode?: string | null
		villageCode?: string | null
	}

	export interface ImportLocalityResult {
		successCount: number
		errorCount: number
		totalCount: number
		createdCount: number
		skippedCount: number
		errors: {
			row: number
			message: string
		}[]
	}

	export interface ImportPersonnelItem {
		/**
		 * Bỏ trống → tự sinh
		 */
		code?: string | null

		fullName: string
		enlistmentDate?: string | null
		recruitment?: string | null
		objectType: string
		rank?: string | null
		position?: string | null
		unitId?: number | null
		unitName?: string | null
		hometown?: string | null
		permanentResidence?: string | null
		userId?: number | null
	}

	export interface ImportPersonnelResult {
		successCount: number
		errorCount: number
		totalCount: number
		errors: {
			row: number
			message: string
		}[]
	}

	export interface LeaveAccess {
		role: LeaveAccessRole
		isAdmin: boolean
		/**
		 * Là chỉ huy CQ của ít nhất 1 đơn vị
		 */
		isCommander: boolean

		/**
		 * Tài khoản Cơ quan quản lý (Cán bộ / Quân lực)
		 */
		isAgency: boolean

		/**
		 * Có hồ sơ quân nhân liên kết
		 */
		isPersonnel: boolean

		/**
		 * Được lập đề xuất phép (admin / chỉ huy / quân nhân, không áp dụng CQQL)
		 */
		canPropose: boolean

		managementArea: string | null
		/**
		 * Đơn vị được phụ trách (leave_units.id); admin = [] nghĩa là tất cả
		 */
		unitIds: number[]

		/**
		 * Tên đơn vị phụ trách
		 */
		unitNames: string[]
	}

	export type LeaveAccessRole =
		| 'admin'
		| 'commander'
		| 'agency'
		| 'personnel'
		| 'none'

	export type LeaveAccountKind = 'personnel' | 'commander' | 'management'

	export type LeaveAlertKind =
		| 'NEED_COMMANDER'
		| 'NEED_AGENCY'
		| 'DECIDED'
		| 'RETURNED'

	export interface LeaveAlertResponse {
		id: number
		createdAt: string
		userId: number
		requestId: number
		kind: LeaveAlertKind
		title: string
		message: string
		readAt: string | null
		/**
		 * Snapshot ngắn từ đơn
		 */
		personnelName?: string | null

		personnelCode?: string | null
		status?: string | null
		totalDays?: number | null
		startDate?: string | null
		endDate?: string | null
	}

	export interface LeaveBatchResponse {
		id: number
		createdAt: string
		updatedAt: string
		requestId: number
		personnelId: number | null
		personnelCode: string | null
		personnelName: string | null
		objectType: string
		leaveType: string
		batchIndex: number
		batchLabel: string
		startDate: string | null
		endDate: string | null
		totalDays: number
		note: string | null
		createdByUserId: number | null
	}

	export interface LeaveClassResponse {
		id: number
		unitId: number
		unitName: string
		name: string
		isActive: boolean
	}

	export interface LeaveRecordResponse {
		id: number
		createdAt: string
		updatedAt: string
		requestId: number
		status: schema.LeaveRequestStatus
		leaveType: schema.LeaveType
		personnelId: number | null
		personnelCode: string | null
		personnelName: string | null
		objectType: schema.LeaveObjectType
		objectTypeLabel: string
		rank: string | null
		position: string | null
		enlistmentDate: string | null
		unitId: number | null
		unitName: string | null
		serviceYears: number
		baseDays: number
		travelDays: number
		extraDays: number
		extraReasons: string[]
		totalDays: number
		startDate: string | null
		endDate: string | null
		localityId: number | null
		localityPath: string | null
		note: string | null
		adminNote: string | null
		proposedByUserId: number | null
		proposedByUsername: string | null
		proposedByDisplayName: string | null
		decidedByUserId: number | null
		decidedByUsername: string | null
		decidedAt: string | null
		archivedAt: string
		requestScope: 'INDIVIDUAL' | 'CLASS' | 'SHORT_LEAVE'
		classId: number | null
		className: string | null
		memberCount: number
	}

	export interface LeaveRequestResponse {
		id: number
		createdAt: string
		updatedAt: string
		leaveType: schema.LeaveType
		requestScope: 'INDIVIDUAL' | 'CLASS' | 'SHORT_LEAVE'
		classId: number | null
		className: string | null
		status: schema.LeaveRequestStatus
		personnelId: number | null
		personnelCode: string | null
		personnelName: string | null
		objectType: schema.LeaveObjectType
		objectTypeLabel: string
		rank: string | null
		position: string | null
		enlistmentDate: string | null
		unitId: number | null
		unitName: string | null
		serviceYears: number
		baseDays: number
		travelDays: number
		extraDays: number
		extraReasons: string[]
		totalDays: number
		startDate: string | null
		endDate: string | null
		localityId: number | null
		localityPath: string | null
		note: string | null
		proposedByUserId: number | null
		proposedByUsername: string | null
		proposedByDisplayName: string | null
		proposerEmail: string | null
		commanderUserId: number | null
		commanderName: string | null
		replacementPersonnelId: number | null
		replacementPersonnelName: string | null
		replacementPosition: string | null
		adminNote: string | null
		decidedByUserId: number | null
		decidedByUsername: string | null
		decidedAt: string | null
		/**
		 * Số ngày đã đi (đơn APPROVED cùng năm, cùng loại)
		 */
		usedDays: number

		/**
		 * Số ngày còn lại theo hạn mức năm (chỉ ANNUAL; SPECIAL = null)
		 */
		remainingDays: number | null

		/**
		 * Hạn mức ngày phép cơ bản năm
		 */
		quotaDays: number | null
	}

	export interface LeaveUnitResponse {
		id: number
		createdAt: string
		updatedAt: string
		code: string | null
		name: string
		parentId: number | null
		level: string | null
		commanderUserId: number | null
		commanderName: string | null
		managementArea: string
		isActive: boolean
	}

	export interface LocalityResponse {
		id: number
		createdAt: string
		updatedAt: string
		name: string
		level: schema.LeaveLocalityLevel
		parentId: number | null
		code: string | null
		children?: LocalityResponse[]
	}

	export interface MailStatus {
		configured: boolean
		mode: 'smtp' | 'smtp-no-auth' | 'ethereal-dev' | 'webhook' | 'none'
		host: string | null
		port: number | null
		user: string | null
		from: string | null
		devMode: boolean
		lastPreviewUrl: string | null
		hint: string
	}

	export interface PersonnelResponse {
		id: number
		createdAt: string
		updatedAt: string
		code: string
		fullName: string
		enlistmentDate: string | null
		recruitment: string | null
		objectType: schema.LeaveObjectType
		rank: string | null
		position: string | null
		classId: number | null
		unitId: number | null
		unitName: string | null
		hometown: string | null
		permanentResidence: string | null
		userId: number | null
		email: string | null
		commanderUserId: number | null
		commanderName: string | null
		className: string | null
		managementArea: string
	}

	export interface PositionResponse {
		id: number
		name: string
		sortOrder: number
		isActive: boolean
	}

	export interface RegulationResponse {
		id: number
		createdAt: string
		updatedAt: string
		leaveType: schema.LeaveType
		objectType: schema.LeaveObjectType | null
		objectTypeLabel: string | null
		minYears: number | null
		maxYears: number | null
		baseDays: number
		label: string | null
		description: string | null
		isActive: boolean
	}

	export interface TakenListResponse {
		id: number
		createdAt: string
		updatedAt: string
		requestId: number
		status: string
		leaveType: string
		personnelId: number | null
		personnelCode: string | null
		personnelName: string | null
		objectType: schema.LeaveObjectType
		objectTypeLabel: string
		rank: string | null
		position: string | null
		enlistmentDate: string | null
		unitId: number | null
		unitName: string | null
		serviceYears: number
		baseDays: number
		travelDays: number
		extraDays: number
		extraReasons: string[]
		totalDays: number
		startDate: string | null
		endDate: string | null
		leaveYear: string
		localityId: number | null
		localityPath: string | null
		note: string | null
		adminNote: string | null
		proposedByUserId: number | null
		proposedByUsername: string | null
		proposedByDisplayName: string | null
		decidedByUserId: number | null
		decidedByUsername: string | null
		decidedAt: string | null
		archivedAt: string
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.AssignLeaveAccount = this.AssignLeaveAccount.bind(this)
			this.CancelLeaveRequest = this.CancelLeaveRequest.bind(this)
			this.CheckYearLeave = this.CheckYearLeave.bind(this)
			this.ComputeLeaveDays = this.ComputeLeaveDays.bind(this)
			this.ConvertLeaveWordTemplate =
				this.ConvertLeaveWordTemplate.bind(this)
			this.CreateLeaveBatch = this.CreateLeaveBatch.bind(this)
			this.CreateLeaveBatchForRequest =
				this.CreateLeaveBatchForRequest.bind(this)
			this.CreateLeaveClass = this.CreateLeaveClass.bind(this)
			this.CreateLeaveLocality = this.CreateLeaveLocality.bind(this)
			this.CreateLeavePersonnel = this.CreateLeavePersonnel.bind(this)
			this.CreateLeavePosition = this.CreateLeavePosition.bind(this)
			this.CreateLeaveRegulation = this.CreateLeaveRegulation.bind(this)
			this.CreateLeaveRequest = this.CreateLeaveRequest.bind(this)
			this.CreateLeaveUnit = this.CreateLeaveUnit.bind(this)
			this.DecideLeaveRequest = this.DecideLeaveRequest.bind(this)
			this.DeleteLeaveBatch = this.DeleteLeaveBatch.bind(this)
			this.DeleteLeaveClass = this.DeleteLeaveClass.bind(this)
			this.DeleteLeaveLocality = this.DeleteLeaveLocality.bind(this)
			this.DeleteLeavePersonnel = this.DeleteLeavePersonnel.bind(this)
			this.DeleteLeavePosition = this.DeleteLeavePosition.bind(this)
			this.DeleteLeaveRegulation = this.DeleteLeaveRegulation.bind(this)
			this.DeleteLeaveUnit = this.DeleteLeaveUnit.bind(this)
			this.GetLeaveAlertCount = this.GetLeaveAlertCount.bind(this)
			this.GetLeaveBatch = this.GetLeaveBatch.bind(this)
			this.GetLeaveMailStatus = this.GetLeaveMailStatus.bind(this)
			this.GetLeaveMeta = this.GetLeaveMeta.bind(this)
			this.GetLeaveMyAccess = this.GetLeaveMyAccess.bind(this)
			this.GetLeavePersonnel = this.GetLeavePersonnel.bind(this)
			this.GetLeaveRecord = this.GetLeaveRecord.bind(this)
			this.GetLeaveRequest = this.GetLeaveRequest.bind(this)
			this.GetMyLeavePersonnel = this.GetMyLeavePersonnel.bind(this)
			this.GetNotYetTakenLeave = this.GetNotYetTakenLeave.bind(this)
			this.GetTakenLeaveList = this.GetTakenLeaveList.bind(this)
			this.ImportLeaveLocalities = this.ImportLeaveLocalities.bind(this)
			this.ImportLeavePersonnel = this.ImportLeavePersonnel.bind(this)
			this.ImportLeaveUnits = this.ImportLeaveUnits.bind(this)
			this.ListLeaveAlerts = this.ListLeaveAlerts.bind(this)
			this.ListLeaveAuditLogs = this.ListLeaveAuditLogs.bind(this)
			this.ListLeaveBatches = this.ListLeaveBatches.bind(this)
			this.ListLeaveClasses = this.ListLeaveClasses.bind(this)
			this.ListLeaveLocalities = this.ListLeaveLocalities.bind(this)
			this.ListLeaveMailLog = this.ListLeaveMailLog.bind(this)
			this.ListLeavePersonnel = this.ListLeavePersonnel.bind(this)
			this.ListLeavePositions = this.ListLeavePositions.bind(this)
			this.ListLeaveRecords = this.ListLeaveRecords.bind(this)
			this.ListLeaveRegulations = this.ListLeaveRegulations.bind(this)
			this.ListLeaveRequests = this.ListLeaveRequests.bind(this)
			this.ListLeaveUnits = this.ListLeaveUnits.bind(this)
			this.MarkLeaveAlertsRead = this.MarkLeaveAlertsRead.bind(this)
			this.PatchLeaveRequest = this.PatchLeaveRequest.bind(this)
			this.SaveLeaveMailConfig = this.SaveLeaveMailConfig.bind(this)
			this.SeedLeaveUnitsFromSystem =
				this.SeedLeaveUnitsFromSystem.bind(this)
			this.TestLeaveMail = this.TestLeaveMail.bind(this)
			this.UpdateLeaveBatch = this.UpdateLeaveBatch.bind(this)
			this.UpdateLeaveLocality = this.UpdateLeaveLocality.bind(this)
			this.UpdateLeavePersonnel = this.UpdateLeavePersonnel.bind(this)
			this.UpdateLeavePosition = this.UpdateLeavePosition.bind(this)
			this.UpdateLeaveRegulation = this.UpdateLeaveRegulation.bind(this)
			this.UpdateLeaveUnit = this.UpdateLeaveUnit.bind(this)
		}

		public async AssignLeaveAccount(params: {
			userId: number
			kind: LeaveAccountKind
			personnelId?: number
			unitId?: number
			managementArea?: 'cán_bộ' | 'quân_lực'
		}): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/accounts/assign`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async CancelLeaveRequest(id: number): Promise<{
			data: LeaveRequestResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/requests/${encodeURIComponent(id)}/cancel`
			)
			return (await resp.json()) as {
				data: LeaveRequestResponse
			}
		}

		public async CheckYearLeave(params: {
			year: number
			search?: string
			unitId?: number
		}): Promise<{
			data: {
				personnelCode: string | null
				personnelName: string | null
				objectType: string
				objectTypeLabel: string
				unitName: string | null
				totalDays: number
				remainingDays: number
				quotaDays: number
			}[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				search: params.search,
				unitId:
					params.unitId === undefined
						? undefined
						: String(params.unitId),
				year: String(params.year)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/reports/check-year`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: {
					personnelCode: string | null
					personnelName: string | null
					objectType: string
					objectTypeLabel: string
					unitName: string | null
					totalDays: number
					remainingDays: number
					quotaDays: number
				}[]
			}
		}

		/**
		 * Tính ngày phép cơ bản theo đối tượng + thâm niên
		 */
		public async ComputeLeaveDays(params: {
			objectType: string
			serviceYears?: number
			enlistmentDate?: string | null
			/**
			 * Ngày bắt đầu nghỉ — dùng tính thâm niên theo tài liệu
			 */
			startDate?: string | null

			leaveType?: string
			travelDays?: number
			extraDays?: number
			/**
			 * Số ngày phép đặc biệt (1–10)
			 */
			specialDays?: number
		}): Promise<{
			data: {
				serviceYears: number
				baseDays: number
				travelDays: number
				extraDays: number
				totalDays: number
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/compute-days`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: {
					serviceYears: number
					baseDays: number
					travelDays: number
					extraDays: number
					totalDays: number
				}
			}
		}

		/**
		 * Chuyển mẫu Word .doc cũ sang .docx để trình duyệt có thể trộn dữ liệu.
		 */
		public async ConvertLeaveWordTemplate(
			params: ConvertWordTemplateRequest
		): Promise<ConvertWordTemplateResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave-management/word-templates/convert`,
				JSON.stringify(params)
			)
			return (await resp.json()) as ConvertWordTemplateResponse
		}

		public async CreateLeaveBatch(params: {
			requestId: number
			personnelId?: number | null
			personnelCode?: string | null
			personnelName?: string | null
			objectType: string
			leaveType: string
			batchIndex?: number
			batchLabel?: string
			startDate?: string | null
			endDate?: string | null
			totalDays?: number
			note?: string | null
		}): Promise<{
			data: LeaveBatchResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/batches`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: LeaveBatchResponse
			}
		}

		public async CreateLeaveBatchForRequest(id: number): Promise<{
			data: LeaveBatchResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/requests/${encodeURIComponent(id)}/batches`
			)
			return (await resp.json()) as {
				data: LeaveBatchResponse
			}
		}

		public async CreateLeaveClass(params: {
			unitId: number
			name: string
		}): Promise<{
			data: LeaveClassResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/classes`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: LeaveClassResponse
			}
		}

		public async CreateLeaveLocality(params: CreateLocalityBody): Promise<{
			data: LocalityResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/localities`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: LocalityResponse
			}
		}

		public async CreateLeavePersonnel(
			params: CreatePersonnelBody
		): Promise<{
			data: PersonnelResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/personnel`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: PersonnelResponse
			}
		}

		public async CreateLeavePosition(params: {
			name: string
			sortOrder?: number
			isActive?: boolean
		}): Promise<{
			data: PositionResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/positions`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: PositionResponse
			}
		}

		public async CreateLeaveRegulation(
			params: CreateRegulationBody
		): Promise<{
			data: RegulationResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/regulations`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RegulationResponse
			}
		}

		public async CreateLeaveRequest(params: CreateLeaveBody): Promise<{
			data: LeaveRequestResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/requests`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: LeaveRequestResponse
			}
		}

		public async CreateLeaveUnit(params: {
			name: string
			code?: string | null
			parentId?: number | null
			level?: string | null
			managementArea?: string
			isActive?: boolean
		}): Promise<{
			data: LeaveUnitResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/units`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: LeaveUnitResponse
			}
		}

		public async DecideLeaveRequest(
			id: number,
			params: {
				/**
				 * APPROVED = duyệt bước hiện tại | RETURNED/REJECTED = trả lại
				 */
				decision: 'APPROVED' | 'REJECTED' | 'RETURNED'

				adminNote?: string | null
				/**
				 * Chỉ huy có thể chỉnh khi duyệt
				 */
				travelDays?: number

				extraDays?: number
				extraReasons?: string[]
			}
		): Promise<{
			data: LeaveRequestResponse
			/**
			 * Kết quả gửi mail tự động (khi duyệt cuối / trả lại)
			 */
			mail?: {
				ok: boolean
				to: string
				mode: string
				error?: string
				previewUrl?: string
				isTestInbox: boolean
				message: string
			} | null
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/requests/${encodeURIComponent(id)}/decide`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: LeaveRequestResponse
				/**
				 * Kết quả gửi mail tự động (khi duyệt cuối / trả lại)
				 */
				mail?: {
					ok: boolean
					to: string
					mode: string
					error?: string
					previewUrl?: string
					isTestInbox: boolean
					message: string
				} | null
			}
		}

		public async DeleteLeaveBatch(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/leave/batches/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteLeaveClass(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/leave/classes/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteLeaveLocality(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/leave/localities/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteLeavePersonnel(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/leave/personnel/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteLeavePosition(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/leave/positions/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteLeaveRegulation(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/leave/regulations/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async DeleteLeaveUnit(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/leave/units/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async GetLeaveAlertCount(): Promise<{
			data: {
				count: number
				pendingApprove: number
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/alerts/unread-count`
			)
			return (await resp.json()) as {
				data: {
					count: number
					pendingApprove: number
				}
			}
		}

		public async GetLeaveBatch(id: number): Promise<{
			data: LeaveBatchResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/batches/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: LeaveBatchResponse
			}
		}

		public async GetLeaveMailStatus(): Promise<{
			data: MailStatus
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/mail/status`
			)
			return (await resp.json()) as {
				data: MailStatus
			}
		}

		/**
		 * Metadata form đề xuất: đối tượng, lý do nghỉ thêm
		 */
		public async GetLeaveMeta(): Promise<{
			data: {
				objectTypes: {
					code: schema.LeaveObjectType
					label: string
				}[]
				extra10Reasons: {
					code: string
					label: string
				}[]
				extra5Reasons: {
					code: string
					label: string
				}[]
				specialReasons: {
					code: string
					label: string
				}[]
				specialMaxDays: number
				specialEligible: schema.LeaveObjectType[]
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/meta`
			)
			return (await resp.json()) as {
				data: {
					objectTypes: {
						code: schema.LeaveObjectType
						label: string
					}[]
					extra10Reasons: {
						code: string
						label: string
					}[]
					extra5Reasons: {
						code: string
						label: string
					}[]
					specialReasons: {
						code: string
						label: string
					}[]
					specialMaxDays: number
					specialEligible: schema.LeaveObjectType[]
				}
			}
		}

		public async GetLeaveMyAccess(): Promise<{
			data: LeaveAccess
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/my-access`
			)
			return (await resp.json()) as {
				data: LeaveAccess
			}
		}

		public async GetLeavePersonnel(id: number): Promise<{
			data: PersonnelResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/personnel/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: PersonnelResponse
			}
		}

		public async GetLeaveRecord(id: number): Promise<{
			data: LeaveRecordResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/records/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: LeaveRecordResponse
			}
		}

		public async GetLeaveRequest(id: number): Promise<{
			data: LeaveRequestResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/requests/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				data: LeaveRequestResponse
			}
		}

		public async GetMyLeavePersonnel(): Promise<{
			data: PersonnelResponse | null
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/my-personnel`
			)
			return (await resp.json()) as {
				data: PersonnelResponse | null
			}
		}

		public async GetNotYetTakenLeave(params: {
			year: number
			objectType?: string
			unitId?: number
		}): Promise<{
			data: {
				personnelId: number | null
				personnelCode: string | null
				personnelName: string | null
				objectType: string
				unitId: number | null
				unitName: string | null
			}[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				objectType: params.objectType,
				unitId:
					params.unitId === undefined
						? undefined
						: String(params.unitId),
				year: String(params.year)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/reports/not-yet-taken`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: {
					personnelId: number | null
					personnelCode: string | null
					personnelName: string | null
					objectType: string
					unitId: number | null
					unitName: string | null
				}[]
			}
		}

		public async GetTakenLeaveList(params: {
			year: number
			leaveType?: string
			objectType?: string
			search?: string
			unitId?: number
		}): Promise<{
			data: TakenListResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				leaveType: params.leaveType,
				objectType: params.objectType,
				search: params.search,
				unitId:
					params.unitId === undefined
						? undefined
						: String(params.unitId),
				year: String(params.year)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/reports/taken-list`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: TakenListResponse[]
			}
		}

		/**
		 * Import địa phương dạng phẳng (file 3321 xã/phường):
		 * mỗi dòng = Tỉnh/TP + Xã/Phường (+ tuỳ chọn Thôn).
		 * Cache in-memory để import ~3k dòng nhanh.
		 */
		public async ImportLeaveLocalities(params: {
			items: ImportLocalityItem[]
		}): Promise<{
			data: ImportLocalityResult
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/localities/import`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ImportLocalityResult
			}
		}

		/**
		 * Import hàng loạt — upsert theo mã (code).
		 */
		public async ImportLeavePersonnel(params: {
			items: ImportPersonnelItem[]
		}): Promise<{
			data: ImportPersonnelResult
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/personnel/import`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: ImportPersonnelResult
			}
		}

		/**
		 * Import hàng loạt đơn vị (theo tên; upsert theo code nếu có)
		 */
		public async ImportLeaveUnits(params: {
			items: {
				name: string
				code?: string | null
			}[]
		}): Promise<{
			data: {
				successCount: number
				errorCount: number
				totalCount: number
				errors: {
					row: number
					message: string
				}[]
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/units/import`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: {
					successCount: number
					errorCount: number
					totalCount: number
					errors: {
						row: number
						message: string
					}[]
				}
			}
		}

		public async ListLeaveAlerts(params: {
			unreadOnly?: boolean
			limit?: number
		}): Promise<{
			data: LeaveAlertResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				limit:
					params.limit === undefined
						? undefined
						: String(params.limit),
				unreadOnly:
					params.unreadOnly === undefined
						? undefined
						: String(params.unreadOnly)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/alerts`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: LeaveAlertResponse[]
			}
		}

		public async ListLeaveAuditLogs(params: {
			entityType?: string
			limit?: number
		}): Promise<void> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				entityType: params.entityType,
				limit:
					params.limit === undefined
						? undefined
						: String(params.limit)
			})

			await this.baseClient.callTypedAPI(
				'GET',
				`/leave/audit-logs`,
				undefined,
				{ query }
			)
		}

		public async ListLeaveBatches(params: { requestId?: number }): Promise<{
			data: LeaveBatchResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				requestId:
					params.requestId === undefined
						? undefined
						: String(params.requestId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/batches`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: LeaveBatchResponse[]
			}
		}

		public async ListLeaveClasses(params: { unitId?: number }): Promise<{
			data: LeaveClassResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				unitId:
					params.unitId === undefined
						? undefined
						: String(params.unitId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/classes`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: LeaveClassResponse[]
			}
		}

		public async ListLeaveLocalities(params: {
			level?: string
			parentId?: number
			tree?: boolean
		}): Promise<{
			data: LocalityResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				level: params.level,
				parentId:
					params.parentId === undefined
						? undefined
						: String(params.parentId),
				tree:
					params.tree === undefined ? undefined : String(params.tree)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/localities`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: LocalityResponse[]
			}
		}

		public async ListLeaveMailLog(params: { limit?: number }): Promise<{
			data: {
				id: number
				createdAt: string
				requestId: number | null
				toEmail: string
				subject: string
				body: string | null
				mode: string | null
				ok: boolean
				error: string | null
				previewUrl: string | null
				kind: string | null
			}[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				limit:
					params.limit === undefined
						? undefined
						: String(params.limit)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/mail/log`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: {
					id: number
					createdAt: string
					requestId: number | null
					toEmail: string
					subject: string
					body: string | null
					mode: string | null
					ok: boolean
					error: string | null
					previewUrl: string | null
					kind: string | null
				}[]
			}
		}

		public async ListLeavePersonnel(params: {
			search?: string
			objectType?: string
		}): Promise<{
			data: PersonnelResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				objectType: params.objectType,
				search: params.search
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/personnel`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: PersonnelResponse[]
			}
		}

		public async ListLeavePositions(params: {
			activeOnly?: boolean
		}): Promise<{
			data: PositionResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				activeOnly:
					params.activeOnly === undefined
						? undefined
						: String(params.activeOnly)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/positions`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: PositionResponse[]
			}
		}

		public async ListLeaveRecords(params: {
			search?: string
			year?: number
			leaveType?: string
			objectType?: string
			status?: string
		}): Promise<{
			data: LeaveRecordResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				leaveType: params.leaveType,
				objectType: params.objectType,
				search: params.search,
				status: params.status,
				year:
					params.year === undefined ? undefined : String(params.year)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/records`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: LeaveRecordResponse[]
			}
		}

		public async ListLeaveRegulations(params: {
			leaveType?: string
		}): Promise<{
			data: RegulationResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				leaveType: params.leaveType
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/regulations`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: RegulationResponse[]
			}
		}

		public async ListLeaveRequests(params: {
			status?: string
			mine?: boolean
			leaveType?: string
			/**
			 * Tìm mã QN hoặc họ tên
			 */
			search?: string

			/**
			 * Lọc theo chỉ huy cơ quan (user id)
			 */
			commanderUserId?: number

			/**
			 * mine — đơn tôi đề xuất
			 * commander — đơn chờ/tôi là chỉ huy cơ quan
			 * agency — đơn chờ cơ quan quản lý (super admin)
			 * all — toàn bộ (super admin) hoặc đơn liên quan (user)
			 */
			inbox?: string
		}): Promise<{
			data: LeaveRequestResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				commanderUserId:
					params.commanderUserId === undefined
						? undefined
						: String(params.commanderUserId),
				inbox: params.inbox,
				leaveType: params.leaveType,
				mine:
					params.mine === undefined ? undefined : String(params.mine),
				search: params.search,
				status: params.status
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/requests`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: LeaveRequestResponse[]
			}
		}

		public async ListLeaveUnits(params: {
			search?: string
			activeOnly?: boolean
		}): Promise<{
			data: LeaveUnitResponse[]
		}> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				activeOnly:
					params.activeOnly === undefined
						? undefined
						: String(params.activeOnly),
				search: params.search
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/leave/units`,
				undefined,
				{ query }
			)
			return (await resp.json()) as {
				data: LeaveUnitResponse[]
			}
		}

		public async MarkLeaveAlertsRead(params: {
			ids?: number[]
			requestId?: number
			all?: boolean
		}): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/alerts/mark-read`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		/**
		 * Chỉ huy CQ được sửa ngày đi đường / nghỉ thêm (khi đơn còn chờ chỉ huy).
		 * Tự tính lại totalDays (+ endDate nếu có startDate).
		 */
		public async PatchLeaveRequest(
			id: number,
			params: {
				travelDays?: number
				extraDays?: number
				extraReasons?: string[]
				startDate?: string | null
				endDate?: string | null
				adminNote?: string | null
			}
		): Promise<{
			data: LeaveRequestResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/leave/requests/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: LeaveRequestResponse
			}
		}

		public async SaveLeaveMailConfig(params: {
			host?: string
			port?: string | number
			user: string
			pass: string
			from?: string
		}): Promise<{
			data: {
				ok: boolean
				message: string
				status: MailStatus
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/mail/config`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: {
					ok: boolean
					message: string
					status: MailStatus
				}
			}
		}

		/**
		 * Seed từ bảng units (tiểu đoàn/đại đội) nếu leave_units trống
		 */
		public async SeedLeaveUnitsFromSystem(): Promise<{
			data: {
				inserted: number
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/units/seed-from-system`
			)
			return (await resp.json()) as {
				data: {
					inserted: number
				}
			}
		}

		public async TestLeaveMail(params: { to?: string }): Promise<{
			data: {
				ok: boolean
				mode: string
				error?: string
				previewUrl?: string
				to: string
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/leave/mail/test`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: {
					ok: boolean
					mode: string
					error?: string
					previewUrl?: string
					to: string
				}
			}
		}

		public async UpdateLeaveBatch(
			id: number,
			params: {
				startDate?: string | null
				endDate?: string | null
				totalDays?: number
				batchLabel?: string
				note?: string | null
			}
		): Promise<{
			data: LeaveBatchResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/leave/batches/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: LeaveBatchResponse
			}
		}

		public async UpdateLeaveLocality(
			id: number,
			params: {
				name?: string
				code?: string | null
			}
		): Promise<{
			data: LocalityResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/leave/localities/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: LocalityResponse
			}
		}

		public async UpdateLeavePersonnel(
			id: number,
			params: {
				code?: string | null
				fullName?: string
				enlistmentDate?: string | null
				recruitment?: string | null
				objectType?: string
				rank?: string | null
				position?: string | null
				classId?: number | null
				unitId?: number | null
				unitName?: string | null
				hometown?: string | null
				permanentResidence?: string | null
				userId?: number | null
				email?: string | null
				commanderUserId?: number | null
				commanderName?: string | null
				className?: string | null
				managementArea?: string
			}
		): Promise<{
			data: PersonnelResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/leave/personnel/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: PersonnelResponse
			}
		}

		public async UpdateLeavePosition(
			id: number,
			params: {
				name?: string
				sortOrder?: number
				isActive?: boolean
			}
		): Promise<{
			data: PositionResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/leave/positions/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: PositionResponse
			}
		}

		public async UpdateLeaveRegulation(
			id: number,
			params: {
				leaveType?: string
				objectType?: string | null
				minYears?: number | null
				maxYears?: number | null
				baseDays?: number
				label?: string | null
				description?: string | null
				isActive?: boolean
			}
		): Promise<{
			data: RegulationResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/leave/regulations/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: RegulationResponse
			}
		}

		public async UpdateLeaveUnit(
			id: number,
			params: {
				name?: string
				code?: string | null
				parentId?: number | null
				level?: string | null
				managementArea?: string
				isActive?: boolean
			}
		): Promise<{
			data: LeaveUnitResponse
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/leave/units/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: LeaveUnitResponse
			}
		}
	}
}

export namespace media {
	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.GetMedia = this.GetMedia.bind(this)
			this.UploadFiles = this.UploadFiles.bind(this)
		}

		public async GetMedia(
			method: 'GET',
			fileUri: string,
			body?: RequestInit['body'],
			options?: CallParameters
		): Promise<globalThis.Response> {
			return this.baseClient.callAPI(
				method,
				`/media/${encodeURIComponent(fileUri)}`,
				body,
				options
			)
		}

		public async UploadFiles(
			method: 'POST',
			body?: RequestInit['body'],
			options?: CallParameters
		): Promise<globalThis.Response> {
			return this.baseClient.callAPI(
				method,
				`/media/upload`,
				body,
				options
			)
		}
	}
}

export namespace notifications {
	export interface GetNotificationsQuery {
		page?: number
		pageSize?: number
	}

	export interface GetNotificationsResponse {
		data: NotificationResponse[]
	}

	export interface GetUnreadCountResponse {
		data: {
			count: number
		}
	}

	export interface Handshake {
		userId: number
	}

	export interface MarkAsReadRequest {
		ids: string[]
	}

	export interface Message {
		type:
			| 'ping'
			| 'birthdayThisWeek'
			| 'birthdayThisMonth'
			| 'birthdayThisQuarter'
			| 'cpvOfficialThisWeek'
			| 'cpvOfficialThisMonth'
			| 'cpvOfficialThisQuarter'
		data: {
			title: string
			message: string
			userId: number
		}
	}

	export interface NotificationItemResponse {
		id: number
		createdAt: string
		updatedAt: string
		notifiableType: 'classes' | 'students'
		notifiableId: number
		notificationId: string
	}

	export interface NotificationResponse {
		id: string
		createdAt: string
		readAt: string
		notificationType:
			| 'birthday'
			| 'officialCpv'
			| 'assetProposal'
			| 'examWorkflow'
		title: string
		message: string
		isBatch: boolean
		batchKey: string
		totalCount: number
		items: NotificationItemResponse[]
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.GetNotifications = this.GetNotifications.bind(this)
			this.GetUnreadCount = this.GetUnreadCount.bind(this)
			this.MarkAllAsRead = this.MarkAllAsRead.bind(this)
			this.MarkAsRead = this.MarkAsRead.bind(this)
			this.NotificationStream = this.NotificationStream.bind(this)
		}

		public async GetNotifications(
			params: GetNotificationsQuery
		): Promise<GetNotificationsResponse> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				page:
					params.page === undefined ? undefined : String(params.page),
				pageSize:
					params.pageSize === undefined
						? undefined
						: String(params.pageSize)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/notifications`,
				undefined,
				{ query }
			)
			return (await resp.json()) as GetNotificationsResponse
		}

		public async GetUnreadCount(): Promise<GetUnreadCountResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/notifications/unread`
			)
			return (await resp.json()) as GetUnreadCountResponse
		}

		public async MarkAllAsRead(): Promise<void> {
			await this.baseClient.callTypedAPI(
				'PATCH',
				`/notifications/mark-all-as-read`
			)
		}

		public async MarkAsRead(params: MarkAsReadRequest): Promise<void> {
			await this.baseClient.callTypedAPI(
				'PATCH',
				`/notifications/mark-as-read`,
				JSON.stringify(params)
			)
		}

		public async NotificationStream(
			params: Handshake
		): Promise<StreamIn<Message>> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				userId: String(params.userId)
			})

			return await this.baseClient.createStreamIn(
				`/notifications/stream`,
				{ query }
			)
		}
	}
}

export namespace permissions {
	export interface CreatePermissionRequest {
		actionId: number
		resourceId: number
	}

	export interface GetPermissionResponse {
		data?: schema.Permission
	}

	export interface GetPermissionsResponse {
		data: schema.Permission[]
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.CreatePermission = this.CreatePermission.bind(this)
			this.GetPermission = this.GetPermission.bind(this)
			this.GetPermissions = this.GetPermissions.bind(this)
		}

		public async CreatePermission(
			params: CreatePermissionRequest
		): Promise<void> {
			await this.baseClient.callTypedAPI(
				'POST',
				`/permissions`,
				JSON.stringify(params)
			)
		}

		public async GetPermission(id: number): Promise<GetPermissionResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/permissions/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as GetPermissionResponse
		}

		public async GetPermissions(): Promise<GetPermissionsResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/permissions`
			)
			return (await resp.json()) as GetPermissionsResponse
		}
	}
}

export namespace resources {
	export interface GetResourcesResponse {
		data: schema.Resource[]
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.GetResources = this.GetResources.bind(this)
		}

		public async GetResources(): Promise<GetResourcesResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI('GET', `/resources`)
			return (await resp.json()) as GetResourcesResponse
		}
	}
}

export namespace roles {
	export interface CreateRoleResponse {
		data: schema.Role
	}

	export interface DeleteRoleResponse {
		ids: number[]
	}

	export interface GetRoleResponse {
		data?: schema.Role
	}

	export interface GetRolesResponse {
		data: schema.Role[]
	}

	export interface UpdateRoleResponse {
		data: schema.Role
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.CreateRole = this.CreateRole.bind(this)
			this.DeleteRoles = this.DeleteRoles.bind(this)
			this.GetRole = this.GetRole.bind(this)
			this.GetRoles = this.GetRoles.bind(this)
			this.UpdateRole = this.UpdateRole.bind(this)
		}

		public async CreateRole(
			params: schema.CreateRoleRequest
		): Promise<CreateRoleResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/roles`,
				JSON.stringify(params)
			)
			return (await resp.json()) as CreateRoleResponse
		}

		public async DeleteRoles(params: {
			ids: number[]
		}): Promise<DeleteRoleResponse> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				ids: params.ids.map((v) => String(v))
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/roles`,
				undefined,
				{ query }
			)
			return (await resp.json()) as DeleteRoleResponse
		}

		public async GetRole(id: number): Promise<GetRoleResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/roles/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as GetRoleResponse
		}

		public async GetRoles(): Promise<GetRolesResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI('GET', `/roles`)
			return (await resp.json()) as GetRolesResponse
		}

		public async UpdateRole(
			id: number,
			params: schema.UpdateRoleRequest
		): Promise<UpdateRoleResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PUT',
				`/roles/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as UpdateRoleResponse
		}
	}
}

export namespace students {
	export interface BulkStudentResponse {
		data: StudentDBResponse[]
	}

	export interface ChildrenInfo {
		fullName: string
		dob: string
	}

	export interface DeleteStudentRequest {
		ids: number[]
	}

	export interface DeleteStudentResponse {
		ids: number[]
	}

	export interface GetPoliticsQualityReportRequest {
		unitIds: number[]
	}

	export interface GetPoliticsQualityReportResponse {
		data: { [key: number]: { [key: string]: any } }
		units: units.Unit[]
	}

	export interface GetStudentsQuery {
		birthdayInMonth?: Month
		birthdayInQuarter?: Quarter
		birthdayInWeek?: boolean
		classId?: number
		hasReligion?: boolean
		ids?: number[]
		isEthnicMinority?: boolean
		isMarried?: boolean
		politicalOrg?: 'hcyu' | 'cpv'
		unitAlias?: string
		unitLevel?: 'battalion' | 'company'
		isCpvOfficialThisWeek?: boolean
		cpvOfficialInMonth?: Month
		cpvOfficialInQuarter?: Quarter
		classIds?: number[]
		withAdversity?: boolean
	}

	export interface GetStudentsResponse {
		data: StudentResponse[]
	}

	export type Month =
		| '01'
		| '02'
		| '03'
		| '04'
		| '05'
		| '06'
		| '07'
		| '08'
		| '09'
		| '10'
		| '11'
		| '12'

	export type Quarter = 'Q1' | 'Q2' | 'Q3' | 'Q4'

	export interface StudentBody {
		fullName: string
		birthPlace: string
		address: string
		dob: string
		rank: string
		previousUnit: string
		previousPosition: string
		ethnic: string
		religion: string
		enlistmentPeriod: string
		politicalOrg: 'hcyu' | 'cpv'
		politicalOrgOfficialDate: string
		cpvId: string | null
		educationLevel: string
		schoolName: string
		major: string
		isGraduated: boolean
		talent: string
		shortcoming: string
		policyBeneficiaryGroup: string
		fatherName: string
		fatherDob: string
		fatherPhoneNumber: string
		fatherJob: string
		/**
		 * fatherJobAddress: string;
		 */
		motherName: string

		motherDob: string
		motherPhoneNumber: string
		motherJob: string
		/**
		 * motherJobAddress: string;
		 */
		isMarried: boolean

		spouseName: string
		spouseDob: string
		spouseJob: string
		spousePhoneNumber: string
		familySize: number
		familyBackground: string
		familyBirthOrder: string
		achievement: string
		disciplinaryHistory: string
		childrenInfos: ChildrenInfo[]
		phone: string
		classId: number
		avatar?: string
		siblings?: ChildrenInfo[]
		contactPerson?: {
			name?: string
			phoneNumber?: string
			address?: string
		}
		studentId?: string
		relatedDocumentations?: string
		status?: 'pending' | 'confirmed'
	}

	export interface StudentBody {
		fullName: string
		birthPlace: string
		address: string
		dob: string
		rank: string
		previousUnit: string
		previousPosition: string
		ethnic: string
		religion: string
		enlistmentPeriod: string
		politicalOrg: 'hcyu' | 'cpv'
		politicalOrgOfficialDate: string
		cpvId: string | null
		educationLevel: string
		schoolName: string
		major: string
		isGraduated: boolean
		talent: string
		shortcoming: string
		policyBeneficiaryGroup: string
		fatherName: string
		fatherDob: string
		fatherPhoneNumber: string
		fatherJob: string
		/**
		 * fatherJobAddress: string;
		 */
		motherName: string

		motherDob: string
		motherPhoneNumber: string
		motherJob: string
		/**
		 * motherJobAddress: string;
		 */
		isMarried: boolean

		spouseName: string
		spouseDob: string
		spouseJob: string
		spousePhoneNumber: string
		familySize: number
		familyBackground: string
		familyBirthOrder: string
		achievement: string
		disciplinaryHistory: string
		childrenInfos: ChildrenInfo[]
		phone: string
		classId: number
		avatar?: string
		siblings?: ChildrenInfo[]
		contactPerson?: {
			name?: string
			phoneNumber?: string
			address?: string
		}
		studentId?: string
		relatedDocumentations?: string
		status?: 'pending' | 'confirmed'
	}

	export interface StudentBulkBody {
		data: StudentBody[]
	}

	export interface StudentDBResponse {
		id: number
		createdAt: string
		updatedAt: string
		fullName: string
		birthPlace: string
		address: string
		dob: string
		rank: string
		previousUnit: string
		previousPosition: string
		ethnic: string
		religion: string
		enlistmentPeriod: string
		politicalOrg: 'hcyu' | 'cpv'
		politicalOrgOfficialDate: string
		cpvId: string | null
		educationLevel: string
		schoolName: string
		major: string
		isGraduated: boolean
		talent: string
		shortcoming: string
		policyBeneficiaryGroup: string
		fatherName: string
		fatherDob: string
		fatherPhoneNumber: string
		fatherJob: string
		/**
		 * fatherJobAddress: string;
		 */
		motherName: string

		motherDob: string
		motherPhoneNumber: string
		motherJob: string
		/**
		 * motherJobAddress: string;
		 */
		isMarried: boolean

		spouseName: string
		spouseDob: string
		spouseJob: string
		spousePhoneNumber: string
		familySize: number
		familyBackground: string
		familyBirthOrder: string
		achievement: string
		disciplinaryHistory: string
		childrenInfos: ChildrenInfo[]
		phone: string
		classId: number
		avatar?: string
		siblings?: ChildrenInfo[]
		contactPerson?: {
			name?: string
			phoneNumber?: string
			address?: string
		}
		studentId?: string
		relatedDocumentations?: string
		status?: 'pending' | 'confirmed'
	}

	export type StudentParamsCronEvent =
		| 'birthdayThisWeek'
		| 'birthdayThisMonth'
		| 'birthdayThisQuarter'
		| 'cpvOfficialThisWeek'
		| 'cpvOfficialThisMonth'
		| 'cpvOfficialThisQuarter'

	export interface StudentResponse {
		class: {
			id: number
			description: string
			name: string
		}
		id: number
		createdAt: string
		updatedAt: string
		fullName: string
		birthPlace: string
		address: string
		dob: string
		rank: string
		previousUnit: string
		previousPosition: string
		ethnic: string
		religion: string
		enlistmentPeriod: string
		politicalOrg: 'hcyu' | 'cpv'
		politicalOrgOfficialDate: string
		cpvId: string | null
		educationLevel: string
		schoolName: string
		major: string
		isGraduated: boolean
		talent: string
		shortcoming: string
		policyBeneficiaryGroup: string
		fatherName: string
		fatherDob: string
		fatherPhoneNumber: string
		fatherJob: string
		/**
		 * fatherJobAddress: string;
		 */
		motherName: string

		motherDob: string
		motherPhoneNumber: string
		motherJob: string
		/**
		 * motherJobAddress: string;
		 */
		isMarried: boolean

		spouseName: string
		spouseDob: string
		spouseJob: string
		spousePhoneNumber: string
		familySize: number
		familyBackground: string
		familyBirthOrder: string
		achievement: string
		disciplinaryHistory: string
		childrenInfos: ChildrenInfo[]
		phone: string
		classId: number
		avatar?: string
		siblings?: ChildrenInfo[]
		contactPerson?: {
			name?: string
			phoneNumber?: string
			address?: string
		}
		studentId?: string
		relatedDocumentations?: string
		status?: 'pending' | 'confirmed'
	}

	export interface UpdatePayload {
		id: number
		fullName?: string
		birthPlace?: string
		address?: string
		dob?: string
		rank?: string
		previousUnit?: string
		previousPosition?: string
		ethnic?: string
		religion?: string
		enlistmentPeriod?: string
		politicalOrg?: 'hcyu' | 'cpv'
		politicalOrgOfficialDate?: string
		cpvId?: string | null
		educationLevel?: string
		schoolName?: string
		major?: string
		isGraduated?: boolean
		talent?: string
		shortcoming?: string
		policyBeneficiaryGroup?: string
		fatherName?: string
		fatherDob?: string
		fatherPhoneNumber?: string
		fatherJob?: string
		motherName?: string
		motherDob?: string
		motherPhoneNumber?: string
		motherJob?: string
		isMarried?: boolean
		spouseName?: string
		spouseDob?: string
		spouseJob?: string
		spousePhoneNumber?: string
		familySize?: number
		familyBackground?: string
		familyBirthOrder?: string
		achievement?: string
		disciplinaryHistory?: string
		childrenInfos?: ChildrenInfo[]
		phone?: string
		classId?: number
		avatar?: string
		siblings?: ChildrenInfo[]
		contactPerson?: {
			name?: string
			phoneNumber?: string
			address?: string
		}
		studentId?: string
		relatedDocumentations?: string
		status?: 'pending' | 'confirmed'
	}

	export interface UpdateStudentBody {
		data: UpdatePayload[]
	}

	export interface UpdateStudentStatusRequest {
		studentIds: number[]
		status: 'pending' | 'confirmed'
	}

	export interface UpdateStudentStatusResponse {
		isSucess: boolean
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.CreateStudent = this.CreateStudent.bind(this)
			this.CreateStudents = this.CreateStudents.bind(this)
			this.DeleteStudents = this.DeleteStudents.bind(this)
			this.ExportPoliticsQualityReport =
				this.ExportPoliticsQualityReport.bind(this)
			this.ExportStudentData = this.ExportStudentData.bind(this)
			this.GetPoliticsQualityReport =
				this.GetPoliticsQualityReport.bind(this)
			this.GetStudents = this.GetStudents.bind(this)
			this.StudentCronjob = this.StudentCronjob.bind(this)
			this.UpdateStudents = this.UpdateStudents.bind(this)
			this.updateStudentStatus = this.updateStudentStatus.bind(this)
		}

		public async CreateStudent(
			params: StudentBody
		): Promise<BulkStudentResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/students`,
				JSON.stringify(params)
			)
			return (await resp.json()) as BulkStudentResponse
		}

		public async CreateStudents(
			params: StudentBulkBody
		): Promise<BulkStudentResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/students/bulk`,
				JSON.stringify(params)
			)
			return (await resp.json()) as BulkStudentResponse
		}

		public async DeleteStudents(
			params: DeleteStudentRequest
		): Promise<DeleteStudentResponse> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				ids: params.ids.map((v) => String(v))
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/students`,
				undefined,
				{ query }
			)
			return (await resp.json()) as DeleteStudentResponse
		}

		public async ExportPoliticsQualityReport(
			method: 'POST',
			body?: RequestInit['body'],
			options?: CallParameters
		): Promise<globalThis.Response> {
			return this.baseClient.callAPI(
				method,
				`/students/politics-quality-report/export`,
				body,
				options
			)
		}

		public async ExportStudentData(
			method: 'POST',
			body?: RequestInit['body'],
			options?: CallParameters
		): Promise<globalThis.Response> {
			return this.baseClient.callAPI(
				method,
				`/students/export`,
				body,
				options
			)
		}

		public async GetPoliticsQualityReport(
			params: GetPoliticsQualityReportRequest
		): Promise<GetPoliticsQualityReportResponse> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				unitIds: params.unitIds.map((v) => String(v))
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/students/politics-quality-report`,
				undefined,
				{ query }
			)
			return (await resp.json()) as GetPoliticsQualityReportResponse
		}

		public async GetStudents(
			params: GetStudentsQuery
		): Promise<GetStudentsResponse> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				birthdayInMonth:
					params.birthdayInMonth === undefined
						? undefined
						: String(params.birthdayInMonth),
				birthdayInQuarter:
					params.birthdayInQuarter === undefined
						? undefined
						: String(params.birthdayInQuarter),
				birthdayInWeek:
					params.birthdayInWeek === undefined
						? undefined
						: String(params.birthdayInWeek),
				classId:
					params.classId === undefined
						? undefined
						: String(params.classId),
				classIds: params.classIds?.map((v) => String(v)),
				cpvOfficialInMonth:
					params.cpvOfficialInMonth === undefined
						? undefined
						: String(params.cpvOfficialInMonth),
				cpvOfficialInQuarter:
					params.cpvOfficialInQuarter === undefined
						? undefined
						: String(params.cpvOfficialInQuarter),
				hasReligion:
					params.hasReligion === undefined
						? undefined
						: String(params.hasReligion),
				ids: params.ids?.map((v) => String(v)),
				isCpvOfficialThisWeek:
					params.isCpvOfficialThisWeek === undefined
						? undefined
						: String(params.isCpvOfficialThisWeek),
				isEthnicMinority:
					params.isEthnicMinority === undefined
						? undefined
						: String(params.isEthnicMinority),
				isMarried:
					params.isMarried === undefined
						? undefined
						: String(params.isMarried),
				politicalOrg:
					params.politicalOrg === undefined
						? undefined
						: String(params.politicalOrg),
				unitAlias: params.unitAlias,
				unitLevel:
					params.unitLevel === undefined
						? undefined
						: String(params.unitLevel),
				withAdversity:
					params.withAdversity === undefined
						? undefined
						: String(params.withAdversity)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/students`,
				undefined,
				{ query }
			)
			return (await resp.json()) as GetStudentsResponse
		}

		public async StudentCronjob(params: {
			event: StudentParamsCronEvent
		}): Promise<void> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				event: String(params.event)
			})

			await this.baseClient.callTypedAPI(
				'GET',
				`/students/cron`,
				undefined,
				{ query }
			)
		}

		public async UpdateStudents(params: UpdateStudentBody): Promise<void> {
			await this.baseClient.callTypedAPI(
				'PATCH',
				`/students`,
				JSON.stringify(params)
			)
		}

		public async updateStudentStatus(
			params: UpdateStudentStatusRequest
		): Promise<UpdateStudentStatusResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PUT',
				`/students/change-status`,
				JSON.stringify(params)
			)
			return (await resp.json()) as UpdateStudentStatusResponse
		}
	}
}

export namespace units {
	export interface CreateUnitRequest {
		data: UnitBody[]
	}

	export interface CreateUnitResponse {
		data: UnitDB[]
	}

	export interface GetUnitRequest {
		id?: number
		name?: string
		level?: 'battalion' | 'company'
		parentId?: number | null
	}

	export interface GetUnitResponse {
		data?: Unit
	}

	export interface GetUnitsQuery {
		level?: 'battalion' | 'company'
	}

	export interface GetUnitsResponse {
		data: Unit[]
	}

	export interface Unit {
		alias: string
		name: string
		level: 'battalion' | 'company'
		id: number
		createdAt: string
		updatedAt: string
		parent: unit | null
		children: Unit[]
		classes: classes.ClassResponse[]
	}

	export interface UnitBody {
		alias: string
		name: string
		level: 'battalion' | 'company'
		parentId?: number | null
	}

	export interface UnitDB {
		alias: string
		name: string
		level: 'battalion' | 'company'
		parentId?: number | null
		id: number
		createdAt: string
		updatedAt: string
	}

	export interface unit {
		alias: string
		name: string
		level: 'battalion' | 'company'
		id: number
		createdAt: string
		updatedAt: string
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.CreateUnit = this.CreateUnit.bind(this)
			this.DeleteUnit = this.DeleteUnit.bind(this)
			this.GetUnit = this.GetUnit.bind(this)
			this.GetUnits = this.GetUnits.bind(this)
			this.UpdateUnit = this.UpdateUnit.bind(this)
		}

		public async CreateUnit(
			params: CreateUnitRequest
		): Promise<CreateUnitResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/units`,
				JSON.stringify(params)
			)
			return (await resp.json()) as CreateUnitResponse
		}

		public async DeleteUnit(id: number): Promise<{
			ok: boolean
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/units/${encodeURIComponent(id)}`
			)
			return (await resp.json()) as {
				ok: boolean
			}
		}

		public async GetUnit(
			alias: string,
			params: GetUnitRequest
		): Promise<GetUnitResponse> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				id: params.id === undefined ? undefined : String(params.id),
				level:
					params.level === undefined
						? undefined
						: String(params.level),
				name: params.name,
				parentId:
					params.parentId === undefined
						? undefined
						: String(params.parentId)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/units/${encodeURIComponent(alias)}`,
				undefined,
				{ query }
			)
			return (await resp.json()) as GetUnitResponse
		}

		public async GetUnits(
			params: GetUnitsQuery
		): Promise<GetUnitsResponse> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				level:
					params.level === undefined
						? undefined
						: String(params.level)
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/units`,
				undefined,
				{ query }
			)
			return (await resp.json()) as GetUnitsResponse
		}

		public async UpdateUnit(
			id: number,
			params: {
				alias?: string
				name?: string
				parentId?: number | null
			}
		): Promise<{
			data: UnitDB
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PATCH',
				`/units/${encodeURIComponent(id)}`,
				JSON.stringify(params)
			)
			return (await resp.json()) as {
				data: UnitDB
			}
		}
	}
}

export namespace user_roles {
	export interface AssignRolesResponse {
		success: boolean
	}

	export interface GetUserRolesResponse {
		roleIds: number[]
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.AssignRolesToUser = this.AssignRolesToUser.bind(this)
			this.GetUserRoles = this.GetUserRoles.bind(this)
		}

		public async AssignRolesToUser(
			params: schema.AssignRoleRequest
		): Promise<AssignRolesResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/user-roles/assign`,
				JSON.stringify(params)
			)
			return (await resp.json()) as AssignRolesResponse
		}

		public async GetUserRoles(
			userId: number
		): Promise<GetUserRolesResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/user-roles/${encodeURIComponent(userId)}`
			)
			return (await resp.json()) as GetUserRolesResponse
		}
	}
}

export namespace users {
	export interface CreateUserRequest {
		username: string
		password: string
		displayName: string
		unitId?: number
		isSuperUser?: boolean
		status?: string
		rank?: string
		position?: string
		alias?: string
		signatureUrl?: string
	}

	export interface CreateUserResponse {
		data: UserDB
	}

	export interface DeleteUserRequest {
		ids: number[]
	}

	export interface DeleteUserResponse {
		ids: number[]
	}

	export interface GetUserResponse {
		data: UserResponse[]
	}

	export interface InitAdminRequest {
		username: string
		password: string
		displayName: string
	}

	export interface InitAdminResponse {
		message: string
	}

	export interface IsInitAdminResponse {
		data: boolean
	}

	export interface RoleDB {
		id: number
		createdAt: string
		updatedAt: string
		name: string
		description?: string
	}

	export interface UpdateUserRequest {
		id: number
		password?: string
		displayName?: string
		unitId?: number
		isSuperUser?: boolean
		rank?: string
		position?: string
		alias?: string
		signatureUrl?: string | null
	}

	export interface UpdateUserResponse {
		data: UserDB
	}

	export interface User {
		roles: RoleDB[]
		id: number
		createdAt: string
		updatedAt: string
		username: string
		displayName: string
		unitId?: number
		isSuperUser?: boolean
		status?: string
		rank?: string
		position?: string
		alias?: string
		signatureUrl?: string
	}

	export interface UserDB {
		id: number
		createdAt: string
		updatedAt: string
		username: string
		displayName: string
		unitId?: number
		isSuperUser?: boolean
		status?: string
		rank?: string
		position?: string
		alias?: string
		signatureUrl?: string
	}

	export interface UserResponse {
		id: number
		createdAt: string
		updatedAt: string
		username: string
		password: string
		displayName: string
		unitId: number
	}

	export class ServiceClient {
		private baseClient: BaseClient

		constructor(baseClient: BaseClient) {
			this.baseClient = baseClient
			this.CreateUser = this.CreateUser.bind(this)
			this.DeleteUsers = this.DeleteUsers.bind(this)
			this.GetPendingPermissionUsers =
				this.GetPendingPermissionUsers.bind(this)
			this.GetUsers = this.GetUsers.bind(this)
			this.InitAdmin = this.InitAdmin.bind(this)
			this.IsInitAdmin = this.IsInitAdmin.bind(this)
			this.UpdateUser = this.UpdateUser.bind(this)
		}

		public async CreateUser(
			params: CreateUserRequest
		): Promise<CreateUserResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/users`,
				JSON.stringify(params)
			)
			return (await resp.json()) as CreateUserResponse
		}

		public async DeleteUsers(
			params: DeleteUserRequest
		): Promise<DeleteUserResponse> {
			// Convert our params into the objects we need for the request
			const query = makeRecord<string, string | string[]>({
				ids: params.ids.map((v) => String(v))
			})

			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'DELETE',
				`/users`,
				undefined,
				{ query }
			)
			return (await resp.json()) as DeleteUserResponse
		}

		/**
		 * User chưa có vai trò (hoặc status pending) — cần admin cấp quyền.
		 * Badge đỏ +N trên Danh sách người dùng.
		 */
		public async GetPendingPermissionUsers(): Promise<{
			data: {
				count: number
				items: {
					userId: number
					username: string
					displayName: string
					status: string | null
					createdAt: string
				}[]
			}
		}> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/users/pending-permissions`
			)
			return (await resp.json()) as {
				data: {
					count: number
					items: {
						userId: number
						username: string
						displayName: string
						status: string | null
						createdAt: string
					}[]
				}
			}
		}

		public async GetUsers(): Promise<GetUserResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI('GET', `/users`)
			return (await resp.json()) as GetUserResponse
		}

		public async InitAdmin(
			params: InitAdminRequest
		): Promise<InitAdminResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'POST',
				`/users/init-admin`,
				JSON.stringify(params)
			)
			return (await resp.json()) as InitAdminResponse
		}

		public async IsInitAdmin(): Promise<IsInitAdminResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'GET',
				`/users/check-init-admin`
			)
			return (await resp.json()) as IsInitAdminResponse
		}

		public async UpdateUser(
			params: UpdateUserRequest
		): Promise<UpdateUserResponse> {
			// Now make the actual call to the API
			const resp = await this.baseClient.callTypedAPI(
				'PUT',
				`/users`,
				JSON.stringify(params)
			)
			return (await resp.json()) as UpdateUserResponse
		}
	}
}

export namespace schema {
	export interface Action {
		name: string
		displayName: string
		description?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}

	export interface AssetMovementLogDB {
		roomAssetId?: number | null
		movementType: string
		executedAt: string
		executingUnit?: string | null
		installAddress?: string | null
		assetCode?: string | null
		assetName: string
		quantity: number
		quantityBefore: number
		quantityAfter: number
		grade: number
		manufactureYear?: number | null
		usageYear?: number | null
		reasonCode?: string | null
		reasonOther?: string | null
		decisionDate?: string | null
		decisionNumber?: string | null
		signer?: string | null
		performer?: string | null
		explanation?: string | null
		note?: string | null
		buildingCode?: string | null
		buildingName?: string | null
		roomCode?: string | null
		roomName?: string | null
		floorName?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}

	/**
	 * INCREASE | DECREASE | ADJUST | TRANSFER (điều động) | RECALL (thu hồi)
	 */
	export type AssetMovementType =
		| 'INCREASE'
		| 'DECREASE'
		| 'ADJUST'
		| 'TRANSFER'
		| 'RECALL'

	export interface AssignRoleRequest {
		userId: number
		roleIds: number[]
	}

	export interface BuildingDB {
		code: string
		name: string
		managerCode?: string | null
		area?: string | null
		address?: string | null
		description?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}

	export interface CreateRoleRequest {
		name: string
		description?: string | null
		permissionIds?: number[]
	}

	export interface InventoryLogDB {
		roomAssetId: number
		inventoryDate: string
		actualQuantity: number
		expectedQuantity: number
		result?: string | null
		note?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}

	/**
	 * Cấp địa phương
	 */
	export type LeaveLocalityLevel = 'province' | 'ward' | 'village'

	/**
	 * Mã đối tượng theo quy định (Ma_DT):
	 * SQ | QNCN | CNQP | VCQP | HSQBS | HV | KHAC
	 * Legacy (map khi đọc/ghi): QN→SQ, CN→CNQP, HSQ|BS→HSQBS
	 */
	export type LeaveObjectType =
		| 'SQ'
		| 'QNCN'
		| 'CNQP'
		| 'VCQP'
		| 'HSQBS'
		| 'HV'
		| 'KHAC'
		| 'QN'
		| 'CN'
		| 'HSQ'
		| 'BS'

	/**
	 * DRAFT — nháp
	 * PENDING_COMMANDER — chờ chỉ huy cơ quan duyệt
	 * PENDING_AGENCY — chờ cơ quan quản lý (in / BGH–quân lực ký)
	 * PENDING — legacy (= chờ duyệt, map như PENDING_COMMANDER)
	 * APPROVED — đã duyệt (sau khi ký)
	 * RETURNED — trả lại
	 * REJECTED — từ chối (legacy, map như RETURNED)
	 * CANCELLED — đã hủy
	 */
	export type LeaveRequestStatus =
		| 'DRAFT'
		| 'PENDING'
		| 'PENDING_COMMANDER'
		| 'PENDING_AGENCY'
		| 'APPROVED'
		| 'RETURNED'
		| 'REJECTED'
		| 'CANCELLED'

	/**
	 * ANNUAL = phép hằng năm | SPECIAL = phép đặc biệt
	 */
	export type LeaveType = 'ANNUAL' | 'SPECIAL'

	export interface Permission {
		resource?: Resource
		action: Action
		name: string
		displayName: string
		description?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}

	export interface RepairLogDB {
		roomAssetId: number
		repairDate: string
		content: string
		cost: number
		performer?: string | null
		note?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}

	export interface ReplacementLogDB {
		roomAssetId: number
		replacementDate: string
		oldAsset: string
		newAsset: string
		reason?: string | null
		performer?: string | null
		note?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}

	export interface Resource {
		permissions: Permission[]
		name: string
		displayName: string
		description?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}

	export interface Role {
		permissions?: Permission[]
		users?: User[]
		name: string
		description?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}

	export interface RoomAssetDB {
		roomId: number
		code?: string | null
		name: string
		category: string
		quantity: number
		brokenQuantity?: number | null
		unit?: string | null
		holdingUnitId?: number | null
		grade?: number | null
		manufactureYear?: number | null
		usageYear?: number | null
		installAddress?: string | null
		status?: string | null
		purchaseDate?: string | null
		expiryDate?: string | null
		brokenAt?: string | null
		repairStartedAt?: string | null
		repairCompletedAt?: string | null
		repairPerformer?: string | null
		description?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}

	export interface RoomImageDB {
		roomId: number
		imageUrl: string
		title?: string | null
		description?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}

	export interface UpdateRoleRequest {
		name?: string
		description?: string | null
		permissionIds?: number[]
	}

	export interface User {
		roles?: Role[]
		unitName?: string
		username: string
		password: string
		displayName: string
		unitId: number | null
		status: 'pending' | 'approved' | null
		isSuperUser: boolean
		rank?: string | null
		position?: string | null
		alias?: string | null
		leaveUnitId?: number | null
		managementArea?: string | null
		signatureUrl?: string | null
		id: number
		createdAt: string
		updatedAt: string
	}
}

function encodeQuery(parts: Record<string, string | string[]>): string {
	const pairs: string[] = []
	for (const key in parts) {
		const val = (
			Array.isArray(parts[key]) ? parts[key] : [parts[key]]
		) as string[]
		for (const v of val) {
			pairs.push(`${key}=${encodeURIComponent(v)}`)
		}
	}
	return pairs.join('&')
}

// makeRecord takes a record and strips any undefined values from it,
// and returns the same record with a narrower type.
// @ts-ignore - TS ignore because makeRecord is not always used
function makeRecord<K extends string | number | symbol, V>(
	record: Record<K, V | undefined>
): Record<K, V> {
	for (const key in record) {
		if (record[key] === undefined) {
			delete record[key]
		}
	}
	return record as Record<K, V>
}

function encodeWebSocketHeaders(headers: Record<string, string>) {
	// url safe, no pad
	const base64encoded = btoa(JSON.stringify(headers))
		.replaceAll('=', '')
		.replaceAll('+', '-')
		.replaceAll('/', '_')
	return 'encore.dev.headers.' + base64encoded
}

class WebSocketConnection {
	public ws: WebSocket

	private hasUpdateHandlers: (() => void)[] = []

	constructor(url: string, headers?: Record<string, string>) {
		let protocols = ['encore-ws']
		if (headers) {
			protocols.push(encodeWebSocketHeaders(headers))
		}

		this.ws = new WebSocket(url, protocols)

		this.on('error', () => {
			this.resolveHasUpdateHandlers()
		})

		this.on('close', () => {
			this.resolveHasUpdateHandlers()
		})
	}

	resolveHasUpdateHandlers() {
		const handlers = this.hasUpdateHandlers
		this.hasUpdateHandlers = []

		for (const handler of handlers) {
			handler()
		}
	}

	async hasUpdate() {
		// await until a new message have been received, or the socket is closed
		await new Promise((resolve) => {
			this.hasUpdateHandlers.push(() => resolve(null))
		})
	}

	on(
		type: 'error' | 'close' | 'message' | 'open',
		handler: (event: any) => void
	) {
		this.ws.addEventListener(type, handler)
	}

	off(
		type: 'error' | 'close' | 'message' | 'open',
		handler: (event: any) => void
	) {
		this.ws.removeEventListener(type, handler)
	}

	close() {
		this.ws.close()
	}
}

export class StreamInOut<Request, Response> {
	public socket: WebSocketConnection
	private buffer: Response[] = []

	constructor(url: string, headers?: Record<string, string>) {
		this.socket = new WebSocketConnection(url, headers)
		this.socket.on('message', (event: any) => {
			this.buffer.push(JSON.parse(event.data))
			this.socket.resolveHasUpdateHandlers()
		})
	}

	close() {
		this.socket.close()
	}

	async send(msg: Request) {
		if (this.socket.ws.readyState === WebSocket.CONNECTING) {
			// await that the socket is opened
			await new Promise((resolve) => {
				this.socket.ws.addEventListener('open', resolve, { once: true })
			})
		}

		return this.socket.ws.send(JSON.stringify(msg))
	}

	async next(): Promise<Response | undefined> {
		for await (const next of this) return next
		return undefined
	}

	async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
		while (true) {
			if (this.buffer.length > 0) {
				yield this.buffer.shift() as Response
			} else {
				if (this.socket.ws.readyState === WebSocket.CLOSED) return
				await this.socket.hasUpdate()
			}
		}
	}
}

export class StreamIn<Response> {
	public socket: WebSocketConnection
	private buffer: Response[] = []

	constructor(url: string, headers?: Record<string, string>) {
		this.socket = new WebSocketConnection(url, headers)
		this.socket.on('message', (event: any) => {
			this.buffer.push(JSON.parse(event.data))
			this.socket.resolveHasUpdateHandlers()
		})
	}

	close() {
		this.socket.close()
	}

	async next(): Promise<Response | undefined> {
		for await (const next of this) return next
		return undefined
	}

	async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
		while (true) {
			if (this.buffer.length > 0) {
				yield this.buffer.shift() as Response
			} else {
				if (this.socket.ws.readyState === WebSocket.CLOSED) return
				await this.socket.hasUpdate()
			}
		}
	}
}

export class StreamOut<Request, Response> {
	public socket: WebSocketConnection
	private responseValue: Promise<Response>

	constructor(url: string, headers?: Record<string, string>) {
		let responseResolver: (_: any) => void
		this.responseValue = new Promise(
			(resolve) => (responseResolver = resolve)
		)

		this.socket = new WebSocketConnection(url, headers)
		this.socket.on('message', (event: any) => {
			responseResolver(JSON.parse(event.data))
		})
	}

	async response(): Promise<Response> {
		return this.responseValue
	}

	close() {
		this.socket.close()
	}

	async send(msg: Request) {
		if (this.socket.ws.readyState === WebSocket.CONNECTING) {
			// await that the socket is opened
			await new Promise((resolve) => {
				this.socket.ws.addEventListener('open', resolve, { once: true })
			})
		}

		return this.socket.ws.send(JSON.stringify(msg))
	}
}
// CallParameters is the type of the parameters to a method call, but require headers to be a Record type
type CallParameters = Omit<RequestInit, 'method' | 'body' | 'headers'> & {
	/** Headers to be sent with the request */
	headers?: Record<string, string>

	/** Query parameters to be sent with the request */
	query?: Record<string, string | string[]>
}

// AuthDataGenerator is a function that returns a new instance of the authentication data required by this API
export type AuthDataGenerator = () =>
	| auth.AuthParams
	| Promise<auth.AuthParams | undefined>
	| undefined

// A fetcher is the prototype for the inbuilt Fetch function
export type Fetcher = typeof fetch

const boundFetch = fetch.bind(this)

class BaseClient {
	readonly baseURL: string
	readonly fetcher: Fetcher
	readonly headers: Record<string, string>
	readonly requestInit: Omit<RequestInit, 'headers'> & {
		headers?: Record<string, string>
	}
	readonly authGenerator?: AuthDataGenerator

	constructor(baseURL: string, options: ClientOptions) {
		this.baseURL = baseURL
		this.headers = {}

		// Add User-Agent header if the script is running in the server
		// because browsers do not allow setting User-Agent headers to requests
		if (!BROWSER) {
			this.headers['User-Agent'] =
				'xyzwe-Generated-TS-Client (Encore/v1.57.9)'
		}

		this.requestInit = options.requestInit ?? {}

		// Setup what fetch function we'll be using in the base client
		if (options.fetcher !== undefined) {
			this.fetcher = options.fetcher
		} else {
			this.fetcher = boundFetch
		}

		// Setup an authentication data generator using the auth data token option
		if (options.auth !== undefined) {
			const auth = options.auth
			if (typeof auth === 'function') {
				this.authGenerator = auth
			} else {
				this.authGenerator = () => auth
			}
		}
	}

	async getAuthData(): Promise<CallParameters | undefined> {
		let authData: auth.AuthParams | undefined

		// If authorization data generator is present, call it and add the returned data to the request
		if (this.authGenerator) {
			const mayBePromise = this.authGenerator()
			if (mayBePromise instanceof Promise) {
				authData = await mayBePromise
			} else {
				authData = mayBePromise
			}
		}

		if (authData) {
			const data: CallParameters = {}

			data.headers = makeRecord<string, string>({
				authorization: authData.authorization
			})

			return data
		}

		return undefined
	}

	// createStreamInOut sets up a stream to a streaming API endpoint.
	async createStreamInOut<Request, Response>(
		path: string,
		params?: CallParameters
	): Promise<StreamInOut<Request, Response>> {
		let { query, headers } = params ?? {}

		// Fetch auth data if there is any
		const authData = await this.getAuthData()

		// If we now have authentication data, add it to the request
		if (authData) {
			if (authData.query) {
				query = { ...query, ...authData.query }
			}
			if (authData.headers) {
				headers = { ...headers, ...authData.headers }
			}
		}

		const queryString = query ? '?' + encodeQuery(query) : ''
		return new StreamInOut(this.baseURL + path + queryString, headers)
	}

	// createStreamIn sets up a stream to a streaming API endpoint.
	async createStreamIn<Response>(
		path: string,
		params?: CallParameters
	): Promise<StreamIn<Response>> {
		let { query, headers } = params ?? {}

		// Fetch auth data if there is any
		const authData = await this.getAuthData()

		// If we now have authentication data, add it to the request
		if (authData) {
			if (authData.query) {
				query = { ...query, ...authData.query }
			}
			if (authData.headers) {
				headers = { ...headers, ...authData.headers }
			}
		}

		const queryString = query ? '?' + encodeQuery(query) : ''
		return new StreamIn(this.baseURL + path + queryString, headers)
	}

	// createStreamOut sets up a stream to a streaming API endpoint.
	async createStreamOut<Request, Response>(
		path: string,
		params?: CallParameters
	): Promise<StreamOut<Request, Response>> {
		let { query, headers } = params ?? {}

		// Fetch auth data if there is any
		const authData = await this.getAuthData()

		// If we now have authentication data, add it to the request
		if (authData) {
			if (authData.query) {
				query = { ...query, ...authData.query }
			}
			if (authData.headers) {
				headers = { ...headers, ...authData.headers }
			}
		}

		const queryString = query ? '?' + encodeQuery(query) : ''
		return new StreamOut(this.baseURL + path + queryString, headers)
	}

	// callTypedAPI makes an API call, defaulting content type to "application/json"
	public async callTypedAPI(
		method: string,
		path: string,
		body?: RequestInit['body'],
		params?: CallParameters
	): Promise<Response> {
		return this.callAPI(method, path, body, {
			...params,
			headers: { 'Content-Type': 'application/json', ...params?.headers }
		})
	}

	// callAPI is used by each generated API method to actually make the request
	public async callAPI(
		method: string,
		path: string,
		body?: RequestInit['body'],
		params?: CallParameters
	): Promise<Response> {
		let { query, headers, ...rest } = params ?? {}
		const init = {
			...this.requestInit,
			...rest,
			method,
			body: body ?? null
		}

		// Merge our headers with any predefined headers
		init.headers = { ...this.headers, ...init.headers, ...headers }

		// Fetch auth data if there is any
		const authData = await this.getAuthData()

		// If we now have authentication data, add it to the request
		if (authData) {
			if (authData.query) {
				query = { ...query, ...authData.query }
			}
			if (authData.headers) {
				init.headers = { ...init.headers, ...authData.headers }
			}
		}

		// Make the actual request
		const queryString = query ? '?' + encodeQuery(query) : ''
		const response = await this.fetcher(
			this.baseURL + path + queryString,
			init
		)

		// handle any error responses
		if (!response.ok) {
			// try and get the error message from the response body
			let body: APIErrorResponse = {
				code: ErrCode.Unknown,
				message: `request failed: status ${response.status}`
			}

			// if we can get the structured error we should, otherwise give a best effort
			try {
				const text = await response.text()

				try {
					const jsonBody = JSON.parse(text)
					if (isAPIErrorResponse(jsonBody)) {
						body = jsonBody
					} else {
						body.message += ': ' + JSON.stringify(jsonBody)
					}
				} catch {
					body.message += ': ' + text
				}
			} catch (e) {
				// otherwise we just append the text to the error message
				body.message += ': ' + String(e)
			}

			throw new APIError(response.status, body)
		}

		return response
	}
}

/**
 * APIErrorDetails represents the response from an Encore API in the case of an error
 */
interface APIErrorResponse {
	code: ErrCode
	message: string
	details?: any
}

function isAPIErrorResponse(err: any): err is APIErrorResponse {
	return (
		err !== undefined &&
		err !== null &&
		isErrCode(err.code) &&
		typeof err.message === 'string' &&
		(err.details === undefined ||
			err.details === null ||
			typeof err.details === 'object')
	)
}

function isErrCode(code: any): code is ErrCode {
	return code !== undefined && Object.values(ErrCode).includes(code)
}

/**
 * APIError represents a structured error as returned from an Encore application.
 */
export class APIError extends Error {
	/**
	 * The HTTP status code associated with the error.
	 */
	public readonly status: number

	/**
	 * The Encore error code
	 */
	public readonly code: ErrCode

	/**
	 * The error details
	 */
	public readonly details?: any

	constructor(status: number, response: APIErrorResponse) {
		// extending errors causes issues after you construct them, unless you apply the following fixes
		super(response.message)

		// set error name as constructor name, make it not enumerable to keep native Error behavior
		// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new.target#new.target_in_constructors
		Object.defineProperty(this, 'name', {
			value: 'APIError',
			enumerable: false,
			configurable: true
		})

		// fix the prototype chain
		if ((Object as any).setPrototypeOf == undefined) {
			;(this as any).__proto__ = APIError.prototype
		} else {
			Object.setPrototypeOf(this, APIError.prototype)
		}

		// capture a stack trace
		if ((Error as any).captureStackTrace !== undefined) {
			;(Error as any).captureStackTrace(this, this.constructor)
		}

		this.status = status
		this.code = response.code
		this.details = response.details
	}
}

/**
 * Typeguard allowing use of an APIError's fields'
 */
export function isAPIError(err: any): err is APIError {
	return err instanceof APIError
}

export enum ErrCode {
	/**
	 * OK indicates the operation was successful.
	 */
	OK = 'ok',

	/**
	 * Canceled indicates the operation was canceled (typically by the caller).
	 *
	 * Encore will generate this error code when cancellation is requested.
	 */
	Canceled = 'canceled',

	/**
	 * Unknown error. An example of where this error may be returned is
	 * if a Status value received from another address space belongs to
	 * an error-space that is not known in this address space. Also
	 * errors raised by APIs that do not return enough error information
	 * may be converted to this error.
	 *
	 * Encore will generate this error code in the above two mentioned cases.
	 */
	Unknown = 'unknown',

	/**
	 * InvalidArgument indicates client specified an invalid argument.
	 * Note that this differs from FailedPrecondition. It indicates arguments
	 * that are problematic regardless of the state of the system
	 * (e.g., a malformed file name).
	 *
	 * This error code will not be generated by the gRPC framework.
	 */
	InvalidArgument = 'invalid_argument',

	/**
	 * DeadlineExceeded means operation expired before completion.
	 * For operations that change the state of the system, this error may be
	 * returned even if the operation has completed successfully. For
	 * example, a successful response from a server could have been delayed
	 * long enough for the deadline to expire.
	 *
	 * The gRPC framework will generate this error code when the deadline is
	 * exceeded.
	 */
	DeadlineExceeded = 'deadline_exceeded',

	/**
	 * NotFound means some requested entity (e.g., file or directory) was
	 * not found.
	 *
	 * This error code will not be generated by the gRPC framework.
	 */
	NotFound = 'not_found',

	/**
	 * AlreadyExists means an attempt to create an entity failed because one
	 * already exists.
	 *
	 * This error code will not be generated by the gRPC framework.
	 */
	AlreadyExists = 'already_exists',

	/**
	 * PermissionDenied indicates the caller does not have permission to
	 * execute the specified operation. It must not be used for rejections
	 * caused by exhausting some resource (use ResourceExhausted
	 * instead for those errors). It must not be
	 * used if the caller cannot be identified (use Unauthenticated
	 * instead for those errors).
	 *
	 * This error code will not be generated by the gRPC core framework,
	 * but expect authentication middleware to use it.
	 */
	PermissionDenied = 'permission_denied',

	/**
	 * ResourceExhausted indicates some resource has been exhausted, perhaps
	 * a per-user quota, or perhaps the entire file system is out of space.
	 *
	 * This error code will be generated by the gRPC framework in
	 * out-of-memory and server overload situations, or when a message is
	 * larger than the configured maximum size.
	 */
	ResourceExhausted = 'resource_exhausted',

	/**
	 * FailedPrecondition indicates operation was rejected because the
	 * system is not in a state required for the operation's execution.
	 * For example, directory to be deleted may be non-empty, an rmdir
	 * operation is applied to a non-directory, etc.
	 *
	 * A litmus test that may help a service implementor in deciding
	 * between FailedPrecondition, Aborted, and Unavailable:
	 *  (a) Use Unavailable if the client can retry just the failing call.
	 *  (b) Use Aborted if the client should retry at a higher-level
	 *      (e.g., restarting a read-modify-write sequence).
	 *  (c) Use FailedPrecondition if the client should not retry until
	 *      the system state has been explicitly fixed. E.g., if an "rmdir"
	 *      fails because the directory is non-empty, FailedPrecondition
	 *      should be returned since the client should not retry unless
	 *      they have first fixed up the directory by deleting files from it.
	 *  (d) Use FailedPrecondition if the client performs conditional
	 *      REST Get/Update/Delete on a resource and the resource on the
	 *      server does not match the condition. E.g., conflicting
	 *      read-modify-write on the same resource.
	 *
	 * This error code will not be generated by the gRPC framework.
	 */
	FailedPrecondition = 'failed_precondition',

	/**
	 * Aborted indicates the operation was aborted, typically due to a
	 * concurrency issue like sequencer check failures, transaction aborts,
	 * etc.
	 *
	 * See litmus test above for deciding between FailedPrecondition,
	 * Aborted, and Unavailable.
	 */
	Aborted = 'aborted',

	/**
	 * OutOfRange means operation was attempted past the valid range.
	 * E.g., seeking or reading past end of file.
	 *
	 * Unlike InvalidArgument, this error indicates a problem that may
	 * be fixed if the system state changes. For example, a 32-bit file
	 * system will generate InvalidArgument if asked to read at an
	 * offset that is not in the range [0,2^32-1], but it will generate
	 * OutOfRange if asked to read from an offset past the current
	 * file size.
	 *
	 * There is a fair bit of overlap between FailedPrecondition and
	 * OutOfRange. We recommend using OutOfRange (the more specific
	 * error) when it applies so that callers who are iterating through
	 * a space can easily look for an OutOfRange error to detect when
	 * they are done.
	 *
	 * This error code will not be generated by the gRPC framework.
	 */
	OutOfRange = 'out_of_range',

	/**
	 * Unimplemented indicates operation is not implemented or not
	 * supported/enabled in this service.
	 *
	 * This error code will be generated by the gRPC framework. Most
	 * commonly, you will see this error code when a method implementation
	 * is missing on the server. It can also be generated for unknown
	 * compression algorithms or a disagreement as to whether an RPC should
	 * be streaming.
	 */
	Unimplemented = 'unimplemented',

	/**
	 * Internal errors. Means some invariants expected by underlying
	 * system has been broken. If you see one of these errors,
	 * something is very broken.
	 *
	 * This error code will be generated by the gRPC framework in several
	 * internal error conditions.
	 */
	Internal = 'internal',

	/**
	 * Unavailable indicates the service is currently unavailable.
	 * This is a most likely a transient condition and may be corrected
	 * by retrying with a backoff. Note that it is not always safe to retry
	 * non-idempotent operations.
	 *
	 * See litmus test above for deciding between FailedPrecondition,
	 * Aborted, and Unavailable.
	 *
	 * This error code will be generated by the gRPC framework during
	 * abrupt shutdown of a server process or network connection.
	 */
	Unavailable = 'unavailable',

	/**
	 * DataLoss indicates unrecoverable data loss or corruption.
	 *
	 * This error code will not be generated by the gRPC framework.
	 */
	DataLoss = 'data_loss',

	/**
	 * Unauthenticated indicates the request does not have valid
	 * authentication credentials for the operation.
	 *
	 * The gRPC framework will generate this error code when the
	 * authentication metadata is invalid or a Credentials callback fails,
	 * but also expect authentication middleware to generate it.
	 */
	Unauthenticated = 'unauthenticated'
}
